# 🎯 ПЛАН: Добавление функционала бронирования в свободный маршрут

**Дата:** 30 ноября 2025 г.  
**Задача:** Добавить шаги бронирования (дата, время, пассажиры, багаж, животные, комментарии, транспорт) в экран "Свободный маршрут"

---

## 📋 Текущее состояние

### ✅ Что уже есть в `IndividualBookingScreen`:
1. **Дата поездки** - выбор даты через `CupertinoDatePicker`
2. **Время отправления** - выбор времени через `CupertinoDatePicker`
3. **Количество пассажиров** - список с возможностью добавления/удаления
4. **Добавить ребёнка** - переключатель + модальное окно
5. **Багаж** - экран выбора `BaggageSelectionScreen` (бесплатно для индивидуального)
6. **Животные** - модальное окно `SimplePetSelectionSheet`
7. **Комментарии** - текстовое поле `CupertinoTextField` (500 символов)
8. **Вид транспорта** - экран выбора `VehicleSelectionScreen` (Седан, Универсал, Минивэн, Микроавтобус)

### ❌ Чего НЕТ в `MainScreen` (Свободный маршрут):
- Сейчас создаётся только `TaxiOrder` с базовыми данными:
  - `fromAddress` и `toAddress` (только адреса)
  - `distanceKm` (расстояние)
  - `priceRub` (цена)
  - `status` (статус заказа)
- НЕТ: даты, времени, пассажиров, багажа, животных, комментариев, транспорта

---

## 🎨 Дизайн решения: Модальное окно пошагового бронирования

### Концепция:
После расчёта маршрута на карте, вместо кнопки "Создать заказ" в центре экрана, будет открываться **модальное окно** с пошаговым заполнением данных бронирования.

### Структура модального окна:

```
┌────────────────────────────────────────┐
│   Бронирование свободного маршрута     │ ← Заголовок
├────────────────────────────────────────┤
│                                        │
│  [Шаг 1 из 8] Дата поездки            │ ← Индикатор прогресса
│                                        │
│  ┌──────────────────────────────────┐ │
│  │                                  │ │
│  │     [Содержимое текущего шага]   │ │ ← Динамический контент
│  │                                  │ │
│  └──────────────────────────────────┘ │
│                                        │
│  [Назад]                 [Далее]      │ ← Кнопки навигации
│                                        │
└────────────────────────────────────────┘
```

### Шаги модального окна:

1. **Шаг 1: Дата поездки**
   - `CupertinoDatePicker` (минимум: сегодня, максимум: +30 дней)
   - Обязательное поле

2. **Шаг 2: Время отправления**
   - `CupertinoDatePicker` (mode: time, 24h format)
   - Обязательное поле
   - Предупреждение о ночной доплате (если >= 22:00)

3. **Шаг 3: Количество пассажиров**
   - Список пассажиров с типом (взрослый/ребёнок)
   - Кнопки добавления/удаления
   - Минимум 1 пассажир, максимум 8

4. **Шаг 4: Дети** (если есть)
   - Переключатель "Добавить ребёнка"
   - При включении: модальное окно с выбором возраста/места

5. **Шаг 5: Багаж**
   - Переход на существующий экран `BaggageSelectionScreen`
   - Указать `isIndividualTrip: true` (багаж бесплатный)
   - Можно пропустить (необязательно)

6. **Шаг 6: Животные**
   - Переключатель "Везу животное"
   - При включении: `SimplePetSelectionSheet`
   - Можно пропустить (необязательно)

7. **Шаг 7: Комментарии**
   - Текстовое поле (макс. 500 символов)
   - Можно пропустить (необязательно)

8. **Шаг 8: Вид транспорта**
   - Переход на существующий экран `VehicleSelectionScreen`
   - Выбор: Седан, Универсал, Минивэн, Микроавтобус
   - Можно пропустить (будет выбран по умолчанию Седан)

9. **Шаг 9: Подтверждение и итоговая цена**
   - Сводка всех выбранных данных
   - Расчёт итоговой стоимости
   - Кнопка "Забронировать за Х ₽"

---

## 🗄️ Изменения в базе данных

### 1. Расширение модели `TaxiOrder` (lib/models/taxi_order.dart)

**НОВЫЕ ПОЛЯ:**
```dart
class TaxiOrder {
  // ... СУЩЕСТВУЮЩИЕ ПОЛЯ (НЕ ТРОГАЕМ) ...
  
  // 🆕 ДОБАВЛЯЕМ (опциональные, для обратной совместимости):
  final DateTime? departureDate;      // Дата поездки
  final String? departureTime;        // Время отправления (HH:mm)
  final String? passengersJson;       // JSON: List<PassengerInfo>
  final String? baggageJson;          // JSON: List<BaggageItem>
  final String? petsJson;             // JSON: List<PetInfo>
  final String? notes;                // Комментарии
  final String? vehicleClass;         // sedan/wagon/minivan/microbus
}
```

### 2. Миграция таблицы SQLite: `orders`

**ALTER TABLE** (версия 3):
```sql
ALTER TABLE orders ADD COLUMN departureDate TEXT;
ALTER TABLE orders ADD COLUMN departureTime TEXT;
ALTER TABLE orders ADD COLUMN passengersJson TEXT;
ALTER TABLE orders ADD COLUMN baggageJson TEXT;
ALTER TABLE orders ADD COLUMN petsJson TEXT;
ALTER TABLE orders ADD COLUMN notes TEXT;
ALTER TABLE orders ADD COLUMN vehicleClass TEXT;
```

**Все поля NULLABLE** → старые заказы остаются валидными!

### 3. Firebase

Используется существующая коллекция `orders`. Новые поля будут `null` у существующих заказов, что обеспечивает обратную совместимость.

---

## 📁 Новые файлы для создания

### 1. `lib/widgets/custom_route_booking_modal.dart`
**Назначение:** Модальное окно пошагового бронирования

```dart
class CustomRouteBookingModal extends StatefulWidget {
  final String fromAddress;
  final String toAddress;
  final double? fromLat;
  final double? fromLon;
  final double? toLat;
  final double? toLon;
  final double? distanceKm;
  final double basePrice;
  
  const CustomRouteBookingModal({
    required this.fromAddress,
    required this.toAddress,
    required this.basePrice,
    // ... другие параметры
  });
}
```

**Функционал:**
- Управление шагами (currentStep, nextStep, previousStep)
- Валидация данных на каждом шаге
- Сохранение промежуточных данных
- Расчёт итоговой цены
- Создание `TaxiOrder` с новыми полями

---

## 🔄 Изменения в существующих файлах

### 1. `lib/models/taxi_order.dart`

**ДОБАВЛЯЕМ новые поля:**
```dart
class TaxiOrder {
  // ... СУЩЕСТВУЮЩИЕ ПОЛЯ (НЕ ТРОГАЕМ) ...
  
  // 🆕 НОВЫЕ ПОЛЯ (опциональные):
  final DateTime? departureDate;
  final String? departureTime;
  final String? passengersJson;  // JSON-строка
  final String? baggageJson;     // JSON-строка
  final String? petsJson;        // JSON-строка
  final String? notes;
  final String? vehicleClass;
  
  // Обновить конструктор, toMap(), fromMap(), copyWith()
}
```

### 2. `lib/services/offline_orders_service.dart`

**Обновить версию базы:**
```dart
version: 3,  // БЫЛО: 2

onUpgrade: (db, oldVersion, newVersion) async {
  if (oldVersion < 2) {
    await db.execute('ALTER TABLE orders ADD COLUMN isSynced INTEGER NOT NULL DEFAULT 0');
  }
  if (oldVersion < 3) {
    // 🆕 ДОБАВЛЯЕМ новые колонки
    await db.execute('ALTER TABLE orders ADD COLUMN departureDate TEXT');
    await db.execute('ALTER TABLE orders ADD COLUMN departureTime TEXT');
    await db.execute('ALTER TABLE orders ADD COLUMN passengersJson TEXT');
    await db.execute('ALTER TABLE orders ADD COLUMN baggageJson TEXT');
    await db.execute('ALTER TABLE orders ADD COLUMN petsJson TEXT');
    await db.execute('ALTER TABLE orders ADD COLUMN notes TEXT');
    await db.execute('ALTER TABLE orders ADD COLUMN vehicleClass TEXT');
  }
}
```

### 3. `lib/features/main_screen.dart`

**Изменить метод `_createOrder()`:**
```dart
Future<void> _createOrder() async {
  // Открываем модальное окно бронирования
  final result = await showCupertinoModalPopup<TaxiOrder>(  // ← TaxiOrder!
    context: context,
    builder: (context) => CustomRouteBookingModal(
      fromAddress: _textFieldControllerFrom.text,
      toAddress: _textFieldControllerTo.text,
      fromPoint: _routePointsManager.fromPoint,
      toPoint: _routePointsManager.toPoint,
      distanceKm: _distanceKm,
      basePrice: _calculation?.individualPrice ?? 0,
      baseCost: _calculation?.baseCost ?? 0,
      costPerKm: _calculation?.costPerKm ?? 0,
    ),
  );
  
  if (result != null) {
    // Сохраняем через СУЩЕСТВУЮЩИЙ сервис
    await OfflineOrdersService.instance.saveOrder(result);
    _showOrderCreatedDialog(result);
  }
}
```



---

## 🎯 План реализации (пошагово)

### Этап 1: Расширение базы данных (1 час)
1. ✅ Добавить новые поля в модель `TaxiOrder`
2. ✅ Обновить `toMap()` и `fromMap()`
3. ✅ Обновить `copyWith()`
4. ✅ Обновить миграцию БД в `OfflineOrdersService` (версия 3)
5. ✅ Протестировать совместимость со старыми заказами

### Этап 2: Модальное окно бронирования (3-4 часа)
1. ✅ Создать `CustomRouteBookingModal` (каркас)
2. ✅ Реализовать навигацию по шагам
3. ✅ Реализовать Шаг 1: Дата поездки
4. ✅ Реализовать Шаг 2: Время отправления
5. ✅ Реализовать Шаг 3: Пассажиры
6. ✅ Реализовать Шаг 4: Дети
7. ✅ Реализовать Шаг 5: Багаж (переход на существующий экран)
8. ✅ Реализовать Шаг 6: Животные (переход на существующий экран)
9. ✅ Реализовать Шаг 7: Комментарии
10. ✅ Реализовать Шаг 8: Транспорт (переход на существующий экран)
11. ✅ Реализовать Шаг 9: Подтверждение и цена

### Этап 3: Интеграция с MainScreen (1-2 часа)
1. ✅ Изменить метод `_createOrder()` в `MainScreen`
2. ✅ Убрать центральную панель с кнопкой (или заменить на "Забронировать")
3. ✅ Добавить обработку результата бронирования
4. ✅ Добавить диалог подтверждения создания заказа

### Этап 4: Отображение заказов (1 час)
1. ✅ Обновить отображение в `OrdersScreen` для новых полей
2. ✅ Обновить `BookingDetailScreen` для показа расширенной информации
3. ✅ Добавить проверку на `null` для опциональных полей

### Этап 5: Тестирование (1-2 часа)
1. ✅ Проверить создание заказа со всеми полями
2. ✅ Проверить сохранение в SQLite
3. ✅ Проверить отображение в списке заказов
4. ✅ Проверить расчёт цены
5. ✅ Проверить валидацию

---

## 💰 Расчёт цены для свободного маршрута

### Базовая цена:
```dart
// Используем существующий PriceCalculatorService
final basePrice = calculation?.individualPrice ?? 0;
```

### Дополнительные расходы:
1. **Ночная доплата** (если время >= 22:00): +2000 ₽
2. **Багаж**: Бесплатно (индивидуальный трансфер)
3. **Животные**: По прайсу из `SimplePetSelectionSheet`
4. **Транспорт**: По прайсу из `VehicleSelectionScreen`

### Формула:
```dart
int totalPrice = basePrice +
                nightSurcharge +
                petPrice +
                vehiclePrice;
```

---

## ✅ Критерии готовности

- [ ] В модель `TaxiOrder` добавлены новые nullable поля
- [ ] Таблица `orders` обновлена (версия 3) с новыми колонками
- [ ] Сервис `OfflineOrdersService` поддерживает новые поля
- [ ] Модальное окно `CustomRouteBookingModal` создано
- [ ] Все 9 шагов реализованы
- [ ] Валидация работает на каждом шаге
- [ ] Расчёт цены корректен
- [ ] Заказы с новыми полями сохраняются в SQLite
- [ ] Старые заказы (без новых полей) продолжают работать
- [ ] Заказы отображаются в существующем `OrdersScreen`
- [ ] Детали заказа показывают новые поля (если есть)
- [ ] Нет критических ошибок компиляции
- [ ] Тестирование пройдено

---

## 🎨 UX/UI детали

### Анимации:
- Плавный переход между шагами (slide left/right)
- Индикатор прогресса (8 точек, текущая - заполненная)
- Валидация с визуальной обратной связью

### Кнопки:
- "Назад" - возврат на предыющий шаг (не закрывает модальное окно)
- "Далее" - переход на следующий шаг
- "Пропустить" - для необязательных шагов
- "Забронировать" - финальная кнопка на шаге 9

### Цвета и стиль:
- Следовать существующему дизайну из `IndividualBookingScreen`
- Использовать `theme.primary` для акцентов
- Cupertino стиль для всех компонентов

---

## 📝 Примечания

1. **Обратная совместимость:** ✅ Все новые поля `nullable` → старые заказы работают!
2. **Миграция:** ✅ Автоматическая через `onUpgrade` (версия 2→3)
3. **Firebase:** ✅ Использует ту же коллекцию, новые поля будут `null` у старых
4. **Производительность:** Модальное окно должно открываться < 300ms
5. **Память:** Использовать `AutoDispose` для контроллеров
6. **Единая система:** ✅ Один `OfflineOrdersService`, одна таблица `orders`, один `OrdersScreen`

---

## 🚀 Следующие шаги после согласования

1. Вы просматриваете обновленный план
2. Даёте обратную связь / предложения
3. Я начинаю реализацию поэтапно
4. Тестируем каждый этап
5. Финальная интеграция и тестирование

---

**Статус:** 🟢 Готов к реализации

**Время реализации:** 6-10 часов

**Приоритет:** Высокий (главный экран приложения)
