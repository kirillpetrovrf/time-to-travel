# Time to Travel Backend - Environment Variables
# ВАЖНО: Этот файл содержит ПРИМЕРЫ значений для разработки
# В продакшене использовать реальные секретные ключи!

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=timetotravel
DB_USER=ttadmin
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION
DB_SSL_MODE=disable
# Для продакшена: DB_SSL_MODE=require

# Connection pool settings
DB_POOL_MIN=2
DB_POOL_MAX=10

# ============================================
# REDIS CONFIGURATION
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# В продакшене обязательно установить пароль!

# ============================================
# JWT AUTHENTICATION
# ============================================
# ВАЖНО: В продакшене использовать случайный 256+ битный ключ!
# Сгенерировать: openssl rand -base64 64
JWT_SECRET=SUPER_SECRET_KEY_CHANGE_ME_IN_PRODUCTION_USE_256_BIT_RANDOM
JWT_ACCESS_EXPIRY=3600
# Access token expires in 1 hour (3600 seconds)

JWT_REFRESH_EXPIRY=604800
# Refresh token expires in 7 days (604800 seconds)

# ============================================
# SERVER CONFIGURATION
# ============================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
ENVIRONMENT=development
# development | staging | production

# CORS settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://titotr.ru
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=INFO
# TRACE | DEBUG | INFO | WARNING | ERROR | CRITICAL

LOG_TO_FILE=false
LOG_FILE_PATH=/var/log/timetotravel/app.log

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60
# 100 requests per minute

# ============================================
# PAYMENT PROVIDERS (Опционально)
# ============================================
# YooKassa
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# Tinkoff
TINKOFF_TERMINAL_KEY=
TINKOFF_SECRET_KEY=

# ============================================
# SMS NOTIFICATIONS (Опционально)
# ============================================
SMS_PROVIDER=
SMS_API_KEY=
SMS_SENDER_NAME=TimeToTravel

# ============================================
# EMAIL (Опционально)
# ============================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@titotr.ru

# ============================================
# MONITORING & ANALYTICS
# ============================================
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# ============================================
# API KEYS (Внешние сервисы)
# ============================================
# Yandex Maps API
YANDEX_MAPS_API_KEY=

# Google Maps API (опционально)
GOOGLE_MAPS_API_KEY=
