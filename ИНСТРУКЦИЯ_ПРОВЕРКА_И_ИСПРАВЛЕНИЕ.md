# 🔧 ИНСТРУКЦИЯ: Проверка и исправление передачи данных

**Дата:** 26 января 2026  
**Цель:** Найти и исправить проблемы с передачей данных клиент → сервер → диспетчер

---

## 🎯 БЫСТРАЯ ДИАГНОСТИКА

### Шаг 1: Проверка PostgreSQL на сервере

```bash
# Подключиться к серверу Selectel (SSH)
ssh ваш_сервер@titotr.ru

# Зайти в PostgreSQL контейнер
docker exec -it postgres psql -U postgres -d timetotravel

# Посмотреть всех пользователей
SELECT id, email, name, phone FROM users;

# Посмотреть все заказы
SELECT order_id, from_address, to_address, status, client_phone, created_at 
FROM orders 
ORDER BY created_at DESC 
LIMIT 10;

# Посмотреть pending заказы (которые должен видеть диспетчер)
SELECT order_id, from_address, to_address, client_name, client_phone, 
       passengers, baggage, created_at
FROM orders 
WHERE status = 'pending';

# Проверить структуру данных в JSONB полях
SELECT order_id, passengers, baggage, pets 
FROM orders 
WHERE passengers IS NOT NULL 
LIMIT 5;
```

**Что искать:**
- ✅ Есть ли заказы в таблице `orders`?
- ✅ Есть ли поле `role` в таблице `users`? (должно быть!)
- ✅ Заполнены ли поля `passengers`, `baggage`, `trip_type`, `direction`?
- ✅ Правильный ли формат JSON в JSONB полях?

---

### Шаг 2: Проверка Backend API

```bash
# Проверить статус backend
curl https://titotr.ru/api/health

# Получить список заказов БЕЗ авторизации (для диспетчера)
curl https://titotr.ru/api/orders

# Получить заказы со статусом pending
curl https://titotr.ru/api/orders?status=pending

# Создать тестовый заказ
curl -X POST https://titotr.ru/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "fromAddress": "Донецк, Артёма 1",
    "toAddress": "Ростов, вокзал",
    "departureTime": "2026-01-28T10:00:00Z",
    "passengerCount": 2,
    "basePrice": 1500,
    "totalPrice": 1700,
    "finalPrice": 1700,
    "tripType": "group",
    "direction": "donetskToRostov",
    "passengers": [{"type": "adult"}, {"type": "child"}],
    "baggage": [{"size": "m", "quantity": 2}]
  }'
```

**Что проверять:**
- ✅ Возвращает ли `/api/orders` список заказов?
- ✅ Сохраняется ли тестовый заказ в базу?
- ✅ Правильно ли парсятся JSON поля?

---

### Шаг 3: Проверка логов Flutter приложения

```bash
# Для Android (если подключён телефон)
adb logcat | grep -E "📤|📥|✅|❌|🔍|API"

# Для iOS (симулятор)
xcrun simctl spawn booted log stream --predicate 'processImagePath contains "Runner"' | grep -E "📤|📥|✅|❌"
```

**Что искать в логах:**
```
📤 [API] Отправка заказа на backend...
📤 [API] От: Донецк...
📤 [API] Цена: 1700.0
✅ [API] Backend вернул успешный ответ
✅ [API] Заказ создан с ID: ORDER-2026-01-797

ИЛИ

❌ [API] Ошибка создания заказа: ...
```

---

## 🛠️ ИСПРАВЛЕНИЕ ПРОБЛЕМ

### ПРОБЛЕМА 1: Диспетчер не видит заказы

**Причина:** Нет поля `role` в таблице `users`

**Решение:**

```sql
-- Подключиться к PostgreSQL
docker exec -it postgres psql -U postgres -d timetotravel

-- Добавить поле role
ALTER TABLE users ADD COLUMN role VARCHAR(50) DEFAULT 'client';

-- Обновить роли существующих пользователей
UPDATE users SET role = 'admin' WHERE email = 'admin@titotr.ru';
UPDATE users SET role = 'dispatcher' WHERE email = 'driver@titotr.ru';

-- Создать пользователя Евгений (если нет)
INSERT INTO users (email, password_hash, name, phone, role, is_verified, is_active)
VALUES (
  'evgeny@titotr.ru',
  '$2a$10$Y7QWLpJ3Z2Y5h5N6N4P8Z.uX5Z5Z5Z5Z5Z5Z5Z5Z5Z5Z5Z5Z5Z5Z5',  -- Хеш для "Test123!"
  'Евгений',
  '+79001112233',
  'dispatcher',
  TRUE,
  TRUE
);

-- Проверить
SELECT id, email, name, role FROM users WHERE role = 'dispatcher';
```

**Проверка в приложении:**
1. Войти как диспетчер
2. Открыть раздел "Заказы"
3. Должны отобразиться ВСЕ заказы из базы

---

### ПРОБЛЕМА 2: Ошибка "coordinates required"

**Причина:** PostgreSQL требует NOT NULL для координат, но клиент их не отправляет

**Решение 1 (быстрое):** Сделать координаты nullable

```sql
ALTER TABLE orders ALTER COLUMN from_lat DROP NOT NULL;
ALTER TABLE orders ALTER COLUMN from_lon DROP NOT NULL;
ALTER TABLE orders ALTER COLUMN to_lat DROP NOT NULL;
ALTER TABLE orders ALTER COLUMN to_lon DROP NOT NULL;
```

**Решение 2 (правильное):** Добавить геокодирование на backend

```dart
// backend/backend/lib/services/geocoder_service.dart
class GeocoderService {
  Future<Point?> geocode(String address) async {
    // Использовать Yandex Geocoder API
    final response = await http.get(
      'https://geocode-maps.yandex.ru/1.x/?apikey=ВАШ_КЛЮЧ&geocode=$address&format=json'
    );
    // Парсить координаты из ответа
  }
}

// В OrderRepository.create():
if (dto.fromLat == null) {
  final point = await geocoder.geocode(dto.fromAddress);
  dto = dto.copyWith(fromLat: point.latitude, fromLon: point.longitude);
}
```

---

### ПРОБЛЕМА 3: JSON данные не парсятся

**Причина:** Несоответствие структуры JSON между клиентом и сервером

**Пример ошибки:**
```
Client отправляет: {"type": "adult"}
Backend ожидает: {"name": "Иван", "age": 30, "type": "adult"}
```

**Проверка:**

```sql
-- Посмотреть что РЕАЛЬНО в базе
SELECT order_id, passengers, baggage FROM orders WHERE passengers IS NOT NULL LIMIT 5;

-- Результат:
-- passengers: [{"type":"adult"},{"type":"child","seatType":"booster"}]
-- baggage: [{"size":"m","quantity":2,"pricePerExtraItem":100.0}]
```

**Решение:** Убедиться что backend и client используют одинаковые модели

```dart
// backend/backend/lib/models/order.dart
@JsonSerializable()
class Passenger {
  final String? type;      // "adult" или "child"
  final String? seatType;  // Для детей: "booster", "seat"
  final int? ageMonths;    // Возраст в месяцах
  // ...
}

// lib/models/passenger_info.dart
enum PassengerType { adult, child }

class PassengerInfo {
  final PassengerType type;
  
  Map<String, dynamic> toJson() => {
    'type': type.toString().split('.').last,  // "adult"
  };
}
```

---

### ПРОБЛЕМА 4: Часовые пояса (время отображается неправильно)

**Причина:** Клиент отправляет UTC, PostgreSQL хранит без timezone

**Решение:**

```sql
-- Изменить тип колонки
ALTER TABLE orders ALTER COLUMN created_at TYPE TIMESTAMP WITH TIME ZONE;
ALTER TABLE orders ALTER COLUMN updated_at TYPE TIMESTAMP WITH TIME ZONE;

-- Пересоздать триггер с timezone
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();  -- NOW() возвращает TIMESTAMP WITH TIME ZONE
    RETURN NEW;
END;
$$ language 'plpgsql';
```

**В коде Flutter:**

```dart
// Всегда отправлять ISO 8601 с timezone
departureTime.toIso8601String()  // "2026-01-28T10:00:00.000Z"

// При получении - конвертировать в локальное время
final localTime = DateTime.parse(apiOrder.departureTime).toLocal();
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Создать тестовый заказ через приложение

1. **Открыть приложение (клиент)**
2. **Выбрать "Групповая поездка"**
3. **Заполнить:**
   - Направление: Донецк → Ростов
   - Дата: Завтра
   - Время: 10:00
   - Пассажиры: 1 взрослый
   - Багаж: 1 средний
4. **Нажать "Забронировать"**

### Проверить в PostgreSQL

```sql
-- Найти последний созданный заказ
SELECT order_id, from_address, to_address, status, passengers, baggage, created_at
FROM orders 
ORDER BY created_at DESC 
LIMIT 1;

-- Должно быть что-то вроде:
-- order_id: ORDER-2026-01-XXX
-- from_address: Донецк...
-- to_address: Ростов...
-- status: pending
-- passengers: [{"type":"adult"}]
-- baggage: [{"size":"m","quantity":1}]
```

### Проверить в приложении диспетчера

1. **Открыть приложение (диспетчер)**
2. **Перейти на вкладку "Заказы"**
3. **Проверить:**
   - ✅ Отображается ли новый заказ?
   - ✅ Правильные ли данные (адреса, цена, пассажиры)?
   - ✅ Можно ли подтвердить заказ?

---

## 📋 ЧЕКЛИСТ ПРОВЕРКИ

- [ ] PostgreSQL запущен и доступен
- [ ] Таблица `users` имеет поле `role`
- [ ] Есть пользователи с ролью `dispatcher`
- [ ] Таблица `orders` содержит заказы
- [ ] Backend API отвечает на `/api/orders`
- [ ] Координаты nullable или геокодирование настроено
- [ ] JSON поля правильно парсятся
- [ ] Часовые пояса настроены
- [ ] Клиент может создавать заказы
- [ ] Диспетчер видит все заказы

---

## 🆘 ЭКСТРЕННАЯ ПОМОЩЬ

### Полный сброс базы данных (ОПАСНО!)

```bash
# Остановить backend
docker compose down

# Удалить все данные
docker volume rm backend_postgres_data

# Пересоздать базу
docker compose up -d postgres

# Применить схему заново
docker cp database/init/01-schema.sql postgres:/tmp/
docker cp database/init/02-seed.sql postgres:/tmp/
docker exec postgres psql -U postgres -c "CREATE DATABASE timetotravel;"
docker exec -i postgres psql -U postgres -d timetotravel < /tmp/01-schema.sql
docker exec -i postgres psql -U postgres -d timetotravel < /tmp/02-seed.sql
```

### Просмотр логов backend

```bash
# Логи Dart Frog backend
docker logs backend-app -f

# Логи PostgreSQL
docker logs postgres -f

# Все логи вместе
docker compose logs -f
```

---

**Если проблема не решена:**
1. Сохраните логи: `docker compose logs > logs.txt`
2. Экспортируйте данные из БД: `pg_dump timetotravel > dump.sql`
3. Опишите проблему подробно с примерами

**Контакты для помощи:**
- Разработчик: admin@titotr.ru
- Заказчик Евгений: evgeny@titotr.ru (если создан в БД)
