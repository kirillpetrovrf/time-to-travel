# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ ИСПРАВЛЕНИЯ

## ✅ Что было исправлено

### 1. **Dio Interceptor** (lib/core/di/service_locator.dart)
- Теперь читает токен из правильного ключа `'auth_access_token_fallback'`
- Добавлен fallback на `'access_token'` для совместимости
- Добавлено логирование для отладки

### 2. **AuthStorageService** (lib/services/auth_storage_service.dart)
- Сохраняет токен в **оба** ключа:
  - `'auth_access_token_fallback'` (основной)
  - `'access_token'` (для Dio interceptor)

### 3. **Backend API** (backend/backend/routes/orders/index.dart)
- Убран опасный fallback "все заказы для неавторизованных"
- Теперь возвращает `401 Unauthorized` если нет токена

---

## 🚀 Инструкция по тестированию

### Подготовка

1. **Остановите приложение** если оно запущено

2. **Перезапустите backend:**
```bash
cd backend
./deploy.sh
```

3. **Очистите приложение:**
```bash
flutter clean
flutter pub get
```

4. **Переустановите приложение** (полное удаление с устройства для очистки SharedPreferences):
```bash
# Android
adb uninstall com.timetotravel.app

# Или вручную: Настройки → Приложения → TimeToTravel → Удалить
```

---

### Тест 1: Кирилл Петров (Клиент)

#### 1.1 Авторизация

1. Запустите приложение:
```bash
flutter run
```

2. Авторизуйтесь через Telegram:
   - Телефон: `+79504455444`
   - Имя: Кирилл Петров
   - Username: @osagowork

3. **Проверьте логи** - должно быть:
```
🔐 [AUTH_INTERCEPTOR] Добавлен токен: eyJhbGciOiJIUzI1NiIs...
```

✅ **Ожидается:** Токен найден и добавлен к запросам

#### 1.2 Создание заказов

1. Создайте **2 тестовых заказа**:
   - Заказ 1: Донецк → Ростов, Индивидуальный
   - Заказ 2: Ростов → Донецк, Попутчик

2. **Проверьте логи backend** - должно быть:
```
userId: ed7093ae-8020-43fd-b2fa-63f4291b3426
user_id в таблице orders: ed7093ae-8020-43fd-b2fa-63f4291b3426
```

✅ **Ожидается:** Заказы привязаны к userId Кирилла

#### 1.3 Просмотр заказов

1. Откройте экран **"Мои заказы"**

2. **Проверьте:**
   - Видите **ТОЛЬКО** 2 своих заказа
   - Не видите заказов других пользователей

✅ **Ожидается:** 2 заказа Кирилла

---

### Тест 2: Анастасия Петрова (Клиент)

#### 2.1 Выход и авторизация

1. **Выйдите** из аккаунта Кирилла:
   - Профиль → Выйти

2. **Авторизуйтесь** через Telegram:
   - Телефон: `+7...` (другой номер)
   - Имя: Анастасия Петрова

3. **Проверьте логи:**
```
🔐 [AUTH_INTERCEPTOR] Добавлен токен: eyJhbGciOiJIUzI1NiIs...
userId: <другой_uuid>
```

✅ **Ожидается:** Новый токен и новый userId

#### 2.2 Просмотр заказов

1. Откройте экран **"Мои заказы"**

2. **Проверьте:**
   - Список **ПУСТОЙ** (0 заказов)
   - НЕТ заказов Кирилла

✅ **Ожидается:** 0 заказов (пустой список)

#### 2.3 Создание своего заказа

1. Создайте **1 заказ**:
   - Заказ 3: Москва → Краснодар, Попутчик

2. **Проверьте логи:**
```
userId: <uuid_Анастасии>
user_id в таблице orders: <uuid_Анастасии>
```

3. Откройте экран **"Мои заказы"**

4. **Проверьте:**
   - Видите **ТОЛЬКО** 1 свой заказ
   - НЕТ заказов Кирилла (2 шт)

✅ **Ожидается:** 1 заказ Анастасии

---

### Тест 3: Диспетчер (Админ)

#### 3.1 Переключение в режим диспетчера

1. **НЕ выходя** из аккаунта Анастасии, перейдите в **Профиль**

2. Нажмите **"Переключиться в режим диспетчера"**

3. Подтвердите переключение

4. **Проверьте:**
   - Нижнее меню изменилось (4 вкладки)
   - Есть вкладка "Заказы" (не "Мои заказы")

✅ **Ожидается:** Режим диспетчера активирован

#### 3.2 Просмотр всех заказов

1. Откройте вкладку **"Заказы"**

2. **Проверьте логи:**
```
🔐 [AUTH_INTERCEPTOR] Добавлен токен: eyJhbGciOiJIUzI1NiIs...
userRole: dispatcher (или admin)
Запрос: orders = await orderRepo.findAll(limit: limit);
```

3. **Проверьте список заказов:**
   - Видите **ВСЕ 3 заказа**:
     - ✅ 2 заказа Кирилла
     - ✅ 1 заказ Анастасии

✅ **Ожидается:** 3 заказа (все клиентов)

---

### Тест 4: Проверка безопасности (без токена)

#### 4.1 Очистка токена вручную

1. **Добавьте тестовый код** в OrdersScreen для проверки:

```dart
// Временно удалить токен для теста
final prefs = await SharedPreferences.getInstance();
await prefs.remove('access_token');
await prefs.remove('auth_access_token_fallback');
await _loadData();
```

2. **Проверьте логи:**
```
⚠️ [AUTH_INTERCEPTOR] Токен не найден в SharedPreferences!
Backend: 401 Unauthorized
Error: Authentication required
```

3. **Проверьте UI:**
   - Должна появиться ошибка
   - Список заказов пустой или ошибка загрузки

✅ **Ожидается:** Ошибка авторизации, данные НЕ загружены

---

## 📊 Ожидаемые результаты

### База данных (после всех тестов)

**Таблица users:**
```sql
id                                  | role       | name
------------------------------------|------------|------------------
ed7093ae-8020-43fd-b2fa-63f4291b... | client     | Кирилл Петров
<uuid_Анастасии>                    | client     | Анастасия Петрова
```

**Таблица orders:**
```sql
id          | user_id                              | from_address | to_address
------------|--------------------------------------|--------------|-------------
order-1     | ed7093ae-8020-43fd-b2fa-63f4291b3426 | Донецк       | Ростов
order-2     | ed7093ae-8020-43fd-b2fa-63f4291b3426 | Ростов       | Донецк
order-3     | <uuid_Анастасии>                     | Москва       | Краснодар
```

✅ **Критично:** Все `user_id` **НЕ NULL** и правильно привязаны!

---

## 🐛 Что делать если тест провален?

### Проблема 1: Токен не добавляется к запросам

**Симптомы:**
```
⚠️ [AUTH_INTERCEPTOR] Токен не найден в SharedPreferences!
```

**Решение:**
1. Проверьте логи сохранения токена:
```dart
💾 [STORAGE] Сохраняем токены:
   • accessToken: eyJhbGciOiJIUzI1NiIs...
   • Метод: SharedPreferences
```

2. Убедитесь что токен сохраняется в оба ключа (код исправлен)

3. Переустановите приложение с очисткой данных

---

### Проблема 2: Клиент видит чужие заказы

**Симптомы:**
- Анастасия видит заказы Кирилла

**Решение:**
1. Проверьте backend логи - какой `userId` приходит в запросе
2. Проверьте что токен содержит правильный `userId`
3. Убедитесь что backend использует `findByUserId()` для клиентов

---

### Проблема 3: Backend возвращает 401 для клиента

**Симптомы:**
```
Backend: 401 Unauthorized
Error: Authentication required
```

**Решение:**
1. Убедитесь что токен отправляется (см. Проблема 1)
2. Проверьте что токен валидный (не истёк)
3. Проверьте backend может распарсить JWT токен

---

## ✅ Критерии успешного теста

- ✅ Кирилл видит ТОЛЬКО свои 2 заказа
- ✅ Анастасия видит ТОЛЬКО свой 1 заказ
- ✅ Диспетчер видит ВСЕ 3 заказа
- ✅ Без токена - ошибка 401 Unauthorized
- ✅ В БД все user_id заполнены правильно

---

## 📝 Отчет о тестировании

После прохождения тестов заполните:

```
Дата: _____________
Тестировщик: _____________

Тест 1 (Кирилл - клиент):
- Авторизация: ✅ / ❌
- Создание заказов: ✅ / ❌  
- Просмотр своих заказов (2 шт): ✅ / ❌

Тест 2 (Анастасия - клиент):
- Авторизация: ✅ / ❌
- Пустой список заказов: ✅ / ❌
- Создание заказа: ✅ / ❌
- Просмотр своего заказа (1 шт): ✅ / ❌

Тест 3 (Диспетчер):
- Переключение режима: ✅ / ❌
- Просмотр всех заказов (3 шт): ✅ / ❌

Тест 4 (Безопасность):
- Без токена получаем 401: ✅ / ❌

Замечания:
_______________________
_______________________
```

---

*Создано: 27 января 2026*
