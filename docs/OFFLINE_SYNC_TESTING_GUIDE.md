# 🔄 Гайд по тестированию оффлайн-синхронизации заказов

## 📋 Что реализовано

### 1. Модель данных (`lib/models/taxi_order.dart`)
- ✅ Добавлено поле `isSynced` (bool) - флаг синхронизации
- ✅ Обновлены методы `toMap()`, `fromMap()`, `copyWith()`
- ✅ По умолчанию все заказы создаются с `isSynced = false`

### 2. База данных SQLite (`lib/services/offline_orders_service.dart`)
- ✅ Схема обновлена до версии 2
- ✅ Добавлена колонка `isSynced INTEGER NOT NULL DEFAULT 0`
- ✅ Миграция для существующих БД: `ALTER TABLE orders ADD COLUMN isSynced`
- ✅ Новые методы:
  - `getUnsyncedOrders()` - получить все несинхронизированные заказы
  - `markAsSynced(orderId)` - пометить заказ как синхронизированный
  - `getUnsyncedCount()` - подсчитать количество несинхронизированных

### 3. Сервис синхронизации (`lib/services/orders_sync_service.dart`)
- ✅ Singleton pattern для глобального доступа
- ✅ Автоматический мониторинг интернета через `connectivity_plus`
- ✅ Метод `startAutoSync()` - запускает автосинхронизацию
- ✅ Метод `syncNow()` - принудительная синхронизация
- ✅ Защита от одновременных запусков синхронизации
- ✅ Подробное логирование всех операций

### 4. Интеграция в приложение (`lib/main.dart`)
- ✅ Firebase инициализируется при старте
- ✅ Автосинхронизация запускается автоматически
- ✅ Graceful degradation если Firebase недоступен

---

## 🧪 Сценарии тестирования

### Сценарий 1: Создание заказа БЕЗ интернета

**Шаги:**
1. Отключите WiFi и мобильный интернет на телефоне
2. Откройте приложение
3. Заполните форму заказа:
   - Откуда: Пермь, улица Ленина 50
   - Куда: Кунгур, площадь Победы 1
   - Дата/время: выберите любое
   - Пассажиры: 2
4. Нажмите "Заказать такси"

**Ожидаемый результат:**
```
✅ Диалог успешного создания заказа
📱 В логах:
   [SQLITE] Заказ сохранен: <orderId>
   ❌ [FIREBASE] Ошибка сохранения: ...
   🔄 [SYNC] Запуск автосинхронизации...
   ⚠️ [SYNC] Нет интернета, синхронизация отложена
```

**Проверка в БД:**
```bash
adb shell
run-as com.timetotravel.app
cd databases
sqlite3 taxi_orders.db
SELECT orderId, fromAddress, toAddress, isSynced FROM orders;
# Должно быть: isSynced = 0
```

---

### Сценарий 2: Автоматическая синхронизация при включении интернета

**Предусловие:** Есть несинхронизированные заказы (из Сценария 1)

**Шаги:**
1. Приложение открыто (или в фоне)
2. Включите WiFi или мобильный интернет

**Ожидаемый результат:**
```
📱 В логах через 2-5 секунд:
   📶 [SYNC] Статус подключения изменился: [ConnectivityResult.wifi]
   ✅ [SYNC] Интернет доступен, запускаем синхронизацию...
   🔄 [SYNC] Найдено X заказов для синхронизации
   📤 [SYNC] Отправка заказа <orderId> в Firebase...
   ✅ [SYNC] Заказ <orderId> синхронизирован
   🎉 [SYNC] Синхронизация завершена: успешно=X, ошибок=0
```

**Проверка в Firebase Console:**
1. Откройте [Firebase Console](https://console.firebase.google.com)
2. Выберите проект Time To Travel
3. Firestore Database → orders
4. Найдите заказ по `orderId`

**Проверка в SQLite:**
```sql
SELECT orderId, isSynced FROM orders WHERE orderId = '<orderId>';
# Должно быть: isSynced = 1
```

---

### Сценарий 3: Создание заказа С интернетом

**Шаги:**
1. Убедитесь что интернет включен
2. Создайте новый заказ

**Ожидаемый результат:**
```
📱 В логах:
   [SQLITE] Заказ сохранен: <orderId>
   [FIREBASE] Заказ сохранен: <orderId>
   🔄 [SYNC] Запуск автосинхронизации...
   ✅ [SYNC] Нет заказов для синхронизации
```

**Проверка:**
- В SQLite сразу `isSynced = 0` (создается всегда как несинхронизированный)
- Firebase получает заказ мгновенно через `_onOrderButtonPressed()`
- Через пару секунд сервис синхронизации обнаружит заказ и пометит как синхронизированный

---

### Сценарий 4: Принудительная синхронизация

**Использование в коде:**
```dart
// В любом месте приложения:
await OrdersSyncService.instance.syncNow();

// Проверить количество несинхронизированных:
final count = await OrdersSyncService.instance.getUnsyncedCount();
print('Несинхронизированных заказов: $count');

// Проверить статус интернета:
final hasInternet = await OrdersSyncService.instance.hasInternetConnection();
print('Интернет: ${hasInternet ? "✅" : "❌"}');
```

---

## 🐛 Отладка через логи

### Ключевые префиксы логов:

- `🔄 [SYNC]` - Операции синхронизации
- `📶 [SYNC]` - Статус интернета
- `📤 [SYNC]` - Отправка в Firebase
- `✅ [SYNC]` - Успешные операции
- `❌ [SYNC]` - Ошибки
- `[SQLITE]` - Операции с локальной БД
- `[FIREBASE]` - Операции с Firestore

### Команда для мониторинга логов:
```bash
adb logcat | grep -E "\[SYNC\]|\[SQLITE\]|\[FIREBASE\]"
```

---

## 📊 Полезные SQL-запросы

### Все заказы и их статус синхронизации:
```sql
SELECT 
  orderId, 
  fromAddress, 
  toAddress, 
  CASE WHEN isSynced = 1 THEN '✅ Синхронизирован' ELSE '⏳ Ожидает' END as status,
  datetime(timestamp/1000, 'unixepoch', 'localtime') as created_at
FROM orders 
ORDER BY timestamp DESC;
```

### Только несинхронизированные заказы:
```sql
SELECT orderId, fromAddress, toAddress 
FROM orders 
WHERE isSynced = 0;
```

### Статистика синхронизации:
```sql
SELECT 
  COUNT(*) as total,
  SUM(CASE WHEN isSynced = 1 THEN 1 ELSE 0 END) as synced,
  SUM(CASE WHEN isSynced = 0 THEN 1 ELSE 0 END) as unsynced
FROM orders;
```

---

## ⚠️ Важные замечания

### 1. Порядок сохранения:
```dart
// В main_screen.dart:
await _offlineService.saveOrder(order);  // Всегда первый (isSynced=false)
await _firebaseService.saveOrder(order); // Второй (если есть интернет)
```

### 2. Двойная синхронизация:
- При создании заказа ОНЛАЙН заказ попадает в Firebase сразу через `_onOrderButtonPressed()`
- Затем через пару секунд OrdersSyncService его снова обнаружит и пометит как синхронизированный
- Это нормально и не создает дубликаты (используется тот же `orderId`)

### 3. Миграция существующих заказов:
- Все старые заказы при первом запуске получат `isSynced = 0`
- Они автоматически синхронизируются при первом включении интернета
- Если они уже есть в Firebase, просто обновятся (идемпотентность)

### 4. Китайские телефоны без Google Services:
- Firebase не инициализируется → catch блок в `main.dart`
- Автосинхронизация не запускается
- Приложение работает в чисто оффлайн режиме (SQLite)

---

## 🚀 Следующие шаги (опционально)

### 1. UI индикатор синхронизации
```dart
// Показывать пользователю количество несинхронизированных заказов
StreamBuilder(
  stream: Stream.periodic(Duration(seconds: 5)),
  builder: (context, _) {
    return FutureBuilder<int>(
      future: OrdersSyncService.instance.getUnsyncedCount(),
      builder: (context, snapshot) {
        if (snapshot.hasData && snapshot.data! > 0) {
          return Text('⏳ Несинхронизировано: ${snapshot.data}');
        }
        return SizedBox.shrink();
      },
    );
  },
)
```

### 2. Retry логика
- Добавить экспоненциальную задержку между попытками
- Хранить количество неудачных попыток в SQLite
- Ограничить максимальное количество попыток

### 3. Батч-синхронизация
- Отправлять несколько заказов одним запросом
- Уменьшит нагрузку при синхронизации большого количества заказов

### 4. Конфликты
- Обработка случая когда заказ изменен и на сервере и локально
- Стратегии: "сервер побеждает", "клиент побеждает", "merge"

---

## ✅ Чеклист готовности к продакшену

- [x] isSynced field добавлен в TaxiOrder
- [x] SQLite миграция (v1 → v2)
- [x] Методы запроса несинхронизированных заказов
- [x] OrdersSyncService реализован
- [x] connectivity_plus добавлен
- [x] Firebase инициализирован в main.dart
- [x] Автосинхронизация запускается при старте
- [x] Логирование всех операций
- [ ] UI индикатор синхронизации (опционально)
- [ ] Ручная кнопка синхронизации (опционально)
- [ ] Тестирование на реальных устройствах
- [ ] Тестирование на китайских телефонах
- [ ] Проверка миграции существующих пользователей

---

## 📞 Техническая поддержка

При возникновении проблем проверьте:
1. Логи через `adb logcat | grep SYNC`
2. Состояние БД через SQLite команды выше
3. Firebase Console → Firestore Database
4. Статус интернета в приложении

**Все готово к тестированию!** 🎉
