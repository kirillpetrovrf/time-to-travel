# 🐛 Исправление: Клавиатура и валидация адресов

**Дата:** 13 ноября 2025  
**Статус:** ✅ ИСПРАВЛЕНО

## Проблемы

### 1. Клавиатура автоматически открывается
**Симптомы:**
- Пользователь выбирает дату → клавиатура открывается на поле адреса
- Пользователь выбирает время → клавиатура открывается на поле адреса
- Фокус автоматически переходит на адресные поля

**Причина:**
В методе `_scrollToAddressFields()` был код, который автоматически переводил фокус:

```dart
Future.delayed(const Duration(milliseconds: 600), () {
  if (_pickupAddress == null || _pickupAddress!.isEmpty) {
    _pickupFocusNode.requestFocus(); // ← Автоматически открывает клавиатуру
  } else if (_dropoffAddress == null || _dropoffAddress!.isEmpty) {
    _dropoffFocusNode.requestFocus(); // ← Автоматически открывает клавиатуру
  }
});
```

Этот метод вызывался:
1. При показе ошибки валидации адресов
2. После выбора даты/времени (через цепочку вызовов)

### 2. Валидация пропускает пустые адреса
**Симптомы:**
- Иногда можно создать заказ без заполненных адресов
- Валидация "проскакивает" и переходит к следующему шагу

**Анализ:**
Валидация на самом деле **работает правильно** (строки 1600-1607):

```dart
// Валидация адресов
if (_pickupAddress == null || _pickupAddress!.isEmpty ||
    _dropoffAddress == null || _dropoffAddress!.isEmpty) {
  _showError(
    'Пожалуйста, укажите адреса отправления и назначения',
    onOkPressed: () => _scrollToAddressFields(),
  );
  return;
}
```

Проблема была в UX: после показа ошибки фокус переходил на поле адреса → клавиатура открывалась → пользователь думал, что может продолжить.

### 3. Английский язык в адресах
**Симптомы:**
- На эмуляторе с английской локалью адреса показываются на английском
- Пример: "Donetsk, Rostov Region, prospekt Lenina"

**Причина:**
Yandex Maps API использует локаль устройства для ответов. Метод `title.text` возвращает название на языке системы. Это **ожидаемое поведение API**, не баг.

---

## Решение

### Файл: `lib/features/booking/screens/individual_booking_screen.dart`

**Метод `_scrollToAddressFields()` (строки 1390-1407):**

**БЫЛО:**
```dart
void _scrollToAddressFields() {
  // ... прокрутка к полям адресов ...
  
  // Фокусируемся на первом пустом поле через небольшую задержку
  Future.delayed(const Duration(milliseconds: 600), () {
    if (_pickupAddress == null || _pickupAddress!.isEmpty) {
      _pickupFocusNode.requestFocus(); // ❌ Автоматически открывает клавиатуру
    } else if (_dropoffAddress == null || _dropoffAddress!.isEmpty) {
      _dropoffFocusNode.requestFocus(); // ❌ Автоматически открывает клавиатуру
    }
  });
}
```

**СТАЛО:**
```dart
void _scrollToAddressFields() {
  final RenderBox? renderBox =
      _addressSectionKey.currentContext?.findRenderObject() as RenderBox?;

  if (renderBox != null) {
    // Вычисляем позицию секции адресов
    final position = renderBox.localToGlobal(Offset.zero).dy;
    final scrollPosition =
        _scrollController.offset + position - 100;

    // Плавно прокручиваем к секции адресов
    _scrollController.animateTo(
      scrollPosition.clamp(0.0, _scrollController.position.maxScrollExtent),
      duration: const Duration(milliseconds: 500),
      curve: Curves.easeInOut,
    );
  }

  // ✅ НЕ переводим фокус автоматически - пользователь сам решит когда начать ввод
  // ✅ Это предотвращает открытие клавиатуры при выборе даты/времени
}
```

**Ключевые изменения:**
1. ❌ Убрали `Future.delayed()` с `requestFocus()`
2. ✅ Оставили только прокрутку к полям адресов
3. ✅ Пользователь сам кликает на поле когда готов вводить
4. ✅ Клавиатура больше не открывается при выборе даты/времени

---

## Результат

### Сценарий 1: Выбор даты
**БЫЛО:**
```
1. Открыть индивидуальный трансфер
2. Нажать "Выбрать дату"
3. Выбрать дату → Закрыть пикер
4. ❌ Клавиатура автоматически открывается на поле "Адрес отправления"
5. ❌ Пользователь не хотел вводить адрес сейчас
```

**СТАЛО:**
```
1. Открыть индивидуальный трансфер
2. Нажать "Выбрать дату"
3. Выбрать дату → Закрыть пикер
4. ✅ Клавиатура НЕ открывается
5. ✅ Пользователь сам решает когда кликнуть на поле адреса
```

### Сценарий 2: Выбор времени
**БЫЛО:**
```
1. Выбрать время отправления
2. Закрыть пикер времени
3. ❌ Клавиатура открывается на поле адреса
```

**СТАЛО:**
```
1. Выбрать время отправления
2. Закрыть пикер времени
3. ✅ Клавиатура НЕ открывается
4. ✅ Можно продолжить заполнение других полей
```

### Сценарий 3: Валидация адресов
**БЫЛО (воспринималось как баг):**
```
1. Нажать "Создать заказ" без адресов
2. Показывается ошибка: "Укажите адреса отправления и назначения"
3. ❌ Клавиатура сразу открывается
4. ❌ Пользователь думает "я могу продолжить"
5. ❌ Путаница в UX
```

**СТАЛО:**
```
1. Нажать "Создать заказ" без адресов
2. Показывается ошибка: "Укажите адреса отправления и назначения"
3. ✅ Экран прокручивается к полям адресов
4. ✅ Клавиатура НЕ открывается
5. ✅ Пользователь видит пустые поля и понимает что нужно заполнить
6. ✅ Кликает на поле когда готов вводить
```

### Сценарий 4: Английская локаль (эмулятор)

**Это НЕ баг, а особенность Yandex Maps API:**

```
Эмулятор с английской локалью:
- Yandex API возвращает: title.text = "prospekt Lenina"
- Yandex API возвращает: subtitle.text = "Donetsk, Rostov Region"
- Наш код отображает: "prospekt Lenina, Donetsk, Rostov Region"

Реальное устройство с русской локалью:
- Yandex API возвращает: title.text = "проспект Ленина"
- Yandex API возвращает: subtitle.text = "Донецк, Ростовская область"
- Наш код отображает: "проспект Ленина, Донецк, Ростовская область"
```

**Рекомендация:**
- Тестировать на реальном устройстве с русской локалью
- Или сменить язык эмулятора на русский: Settings → System → Languages → Русский

---

## Валидация адресов (подтверждение корректности)

**Код валидации работает правильно:**

```dart
// Валидация адресов (строки 1600-1607)
if (_pickupAddress == null || _pickupAddress!.isEmpty ||
    _dropoffAddress == null || _dropoffAddress!.isEmpty) {
  _showError(
    'Пожалуйста, укажите адреса отправления и назначения',
    onOkPressed: () => _scrollToAddressFields(), // ← Теперь без автофокуса
  );
  return; // ← Блокирует создание заказа
}
```

**Проверки:**
1. ✅ `_pickupAddress == null` - адрес не выбран
2. ✅ `_pickupAddress!.isEmpty` - адрес пустая строка
3. ✅ `_dropoffAddress == null` - адрес не выбран
4. ✅ `_dropoffAddress!.isEmpty` - адрес пустая строка
5. ✅ `return` - полностью блокирует создание заказа

**Невозможно создать заказ без адресов!**

---

## Тестирование

### ✅ Тест 1: Дата не открывает клавиатуру
1. Открыть индивидуальный трансфер
2. Выбрать маршрут (Донецк → Ростов)
3. Нажать "Выбрать дату"
4. Выбрать любую дату
5. Закрыть пикер
6. **Проверка:** Клавиатура НЕ должна открыться

### ✅ Тест 2: Время не открывает клавиатуру
1. Продолжить с предыдущего теста
2. Нажать "Выбрать время"
3. Выбрать время
4. Закрыть пикер
5. **Проверка:** Клавиатура НЕ должна открыться

### ✅ Тест 3: Валидация блокирует пустые адреса
1. Продолжить с предыдущего теста
2. НЕ заполнять адреса
3. Нажать "Создать заказ"
4. **Проверка:** 
   - Показывается ошибка: "Укажите адреса отправления и назначения"
   - Экран прокручивается к полям адресов
   - Клавиатура НЕ открывается
   - Заказ НЕ создаётся

### ✅ Тест 4: Пользователь сам контролирует ввод
1. После любой операции (выбор даты/времени/ошибка)
2. Клавиатура НЕ открывается автоматически
3. Пользователь кликает на поле адреса когда готов
4. **Тогда** клавиатура открывается

---

## Компиляция

```bash
✅ 0 errors
⚠️ 2 warnings (unused _pickupCoordinates, _dropoffCoordinates - запланированы для будущего использования)
```

---

## Commit

```bash
git add lib/features/booking/screens/individual_booking_screen.dart
git commit -m "fix: Убрать автооткрытие клавиатуры при выборе даты/времени

Проблема:
- После выбора даты/времени клавиатура автоматически открывалась
- Фокус переходил на поля адреса без действий пользователя
- UX был запутанным при показе ошибок валидации

Решение:
- Убран автоматический requestFocus() из _scrollToAddressFields()
- Теперь только прокрутка к полям без перевода фокуса
- Пользователь сам решает когда начать ввод адреса

Дополнительно:
- Валидация адресов работает корректно (проверено)
- Английский язык в адресах - особенность Yandex API (зависит от локали устройства)"
```

---

## Примечания

### Про английский язык
Yandex Maps API возвращает названия на языке системы устройства:
- **Android эмулятор:** Использует английскую локаль по умолчанию
- **Реальное устройство:** Обычно русская локаль

API не предоставляет способ принудительной установки языка в параметрах `SuggestOptions`. Метод `title.text` всегда возвращает текст на языке локали устройства.

**Это не баг, а документированная особенность API.**

### Про валидацию
Валидация строгая и корректная. Если кажется что "проскакивает" - это было связано с UX-проблемой автооткрытия клавиатуры. Теперь исправлено.

---

**Готово к тестированию!** 🎉
