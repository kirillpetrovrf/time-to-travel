# 🔥 Исправление прав доступа Firebase для групп маршрутов

## ❌ Проблема
При создании новой группы появляется ошибка:
```
PERMISSION_DENIED: Permission denied on resource project time-to-travel-dn
```

Это означает, что правила безопасности Firestore запрещают запись в коллекцию `route_groups`.

## ✅ Решение

### Шаг 1: Откройте Firebase Console
1. Перейдите на https://console.firebase.google.com/
2. Выберите проект **time-to-travel-dn**
3. В левом меню выберите **Firestore Database**
4. Перейдите на вкладку **Правила** (Rules)

### Шаг 2: Обновите правила безопасности

Найдите текущие правила (они могут выглядеть примерно так):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Запрет по умолчанию
    match /{document=**} {
      allow read, write: if false;
    }
    
    // Возможно уже есть правила для routes
    match /routes/{routeId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

**Добавьте** следующие правила для коллекции `route_groups`:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Правила для маршрутов (если есть)
    match /routes/{routeId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // 🆕 ДОБАВЬТЕ ЭТИ ПРАВИЛА для групп маршрутов
    match /route_groups/{groupId} {
      // Чтение доступно всем (нужно для отображения в админке)
      allow read: if true;
      
      // Запись доступна только аутентифицированным пользователям
      allow write: if request.auth != null;
      
      // Или если хотите разрешить всем (только для тестирования!):
      // allow write: if true;
    }
    
    // Остальные коллекции - запрет по умолчанию
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

### Шаг 3: Опубликуйте правила
1. Нажмите кнопку **"Опубликовать"** (Publish) в правом верхнем углу
2. Подтвердите изменения

### Шаг 4: Проверьте результат
1. Перезапустите приложение (или выполните Hot Restart)
2. Откройте **Админ панель** → **Фиксированные цены** → **Управление группами**
3. Нажмите **"+"** для создания новой группы
4. Заполните форму и нажмите **"Сохранить"**

## 🔒 Варианты безопасности

### Вариант 1: Только для аутентифицированных (рекомендуется)
```javascript
match /route_groups/{groupId} {
  allow read: if true;
  allow write: if request.auth != null;
}
```
**Требует:** Пользователь должен быть залогинен через Firebase Authentication

### Вариант 2: Только для администраторов (самый безопасный)
```javascript
match /route_groups/{groupId} {
  allow read: if true;
  allow write: if request.auth != null && 
               get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
}
```
**Требует:** Поле `role: 'admin'` в документе пользователя в коллекции `users`

### Вариант 3: Открытый доступ (только для разработки!)
```javascript
match /route_groups/{groupId} {
  allow read, write: if true;
}
```
**⚠️ НЕ используйте в продакшене!** Любой может изменять данные.

## 📊 Проверка в консоли Firebase

После публикации правил вы должны увидеть коллекцию `route_groups` в Firestore Database с документами:

```
route_groups/
  ├─ group_1234567890123/
  │  ├─ name: "Донецк - Ростов-на-Дону"
  │  ├─ basePrice: 8000
  │  ├─ originCities: ["Донецк", "Макеевка"]
  │  ├─ destinationCities: ["Ростов", "Ростов-на-Дону"]
  │  ├─ autoGenerateReverse: true
  │  └─ createdAt: timestamp
```

## 🔍 Дополнительная диагностика

Если проблема сохраняется, проверьте в логах Flutter:

```
I/flutter: 📝 Создаем новую группу:
I/flutter:    ID: group_1733420123456
I/flutter:    Название: Донецк - Ростов
I/flutter:    Цена: 8000₽
I/flutter: ✅ Создана группа: Донецк - Ростов
```

Если видите:
```
I/flutter: ❌ Ошибка создания группы: [cloud_firestore/permission-denied]
I/flutter:    ⚠️ Проблема с правами доступа Firebase!
```

Значит правила еще не обновлены или не опубликованы.

## 📝 Что было исправлено в коде

1. **`groups_management_screen.dart`**: Изменен `_createGroup()` для использования `createGroup()` вместо `updateGroup()`
2. **`route_group_service.dart`**: Добавлено подробное логирование и обработка ошибок PERMISSION_DENIED
3. **`updateGroup()`**: Изменен на использование `.set()` с `merge: true` вместо `.update()`

## ✅ После исправления

Вы сможете:
- ✅ Создавать новые группы через UI
- ✅ Редактировать существующие группы
- ✅ Удалять группы
- ✅ Группы сохраняются в Firebase и видны после перезапуска приложения
