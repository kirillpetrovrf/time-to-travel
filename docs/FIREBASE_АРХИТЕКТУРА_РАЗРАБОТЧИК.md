# 🏗️ АРХИТЕКТУРА: Клиент ↔ Сервер (Firebase)

## 📊 ТЕКУЩАЯ РЕАЛИЗАЦИЯ

### Уровень 1: Локальное хранилище (SQLite)

```
📱 ПРИЛОЖЕНИЕ
├── lib/services/offline_orders_service.dart
│   └── Методы:
│       ├── saveOrder(order)              // Сохранить в SQLite
│       ├── getAllOrders()                // Получить все
│       ├── getUnsyncedOrders()           // Получить несинхронизированные
│       ├── markAsSynced(orderId)         // Пометить как синхронизированный
│       └── getUnsyncedCount()            // Количество несинхронизированных
│
└── База данных: SQLite
    └── Таблица: orders
        └── Поля:
            ├── orderId (PRIMARY KEY)
            ├── userId
            ├── timestamp
            ├── status
            ├── tripType
            ├── fromAddress, toAddress
            ├── fromPoint, toPoint (JSON)
            ├── distanceKm
            ├── finalPrice
            ├── departureDate, departureTime
            ├── passengers (JSON)
            ├── baggage (JSON)
            ├── pets (JSON)
            ├── vehicleClass
            ├── notes
            ├── isSynced (0 или 1) ⭐
            └── createdAt
```

### Уровень 2: Облачное хранилище (Firebase Firestore)

```
☁️ FIREBASE CLOUD
├── lib/services/firebase_orders_service.dart
│   └── Методы:
│       ├── saveOrder(order)              // Сохранить в Firestore
│       ├── getOrdersStream()             // Реал-тайм подписка
│       ├── getOrderById(orderId)         // Получить по ID
│       ├── updateOrderStatus(id, status) // Обновить статус
│       ├── deleteOrder(orderId)          // Удалить
│       ├── getAllOrders()                // Получить все (разовый запрос)
│       └── getOrdersByStatus(status)     // Фильтр по статусу
│
└── Firestore Database
    └── Коллекция: orders
        └── Документы: {orderId}
            └── Все те же поля что и в SQLite
```

### Уровень 3: Синхронизация (OrdersSyncService)

```
🔄 АВТОСИНХРОНИЗАЦИЯ
└── lib/services/orders_sync_service.dart
    ├── Мониторинг интернета:
    │   └── connectivity_plus → отслеживает появление Wi-Fi/Mobile
    │
    ├── Автозапуск при старте:
    │   └── main.dart → OrdersSyncService.instance.startAutoSync()
    │
    └── Логика синхронизации:
        1. При появлении интернета:
           ├── Получить список несинхронизированных из SQLite
           ├── Отправить каждый в Firebase
           └── Пометить как синхронизированный в SQLite
        
        2. При создании нового заказа:
           ├── Сохранить в SQLite (isSynced = 0)
           ├── ПОПЫТКА отправить в Firebase (timeout 3 сек)
           │   ├── Успешно → пометить isSynced = 1
           │   └── Ошибка → оставить isSynced = 0, попытка позже
           └── Продолжить работу приложения
```

---

## 🔄 ПОТОКИ ДАННЫХ

### Поток 1: Создание заказа клиентом

```
┌──────────────────────────────────────────────────────┐
│ 1. КЛИЕНТ создаёт заказ                              │
│    lib/features/main_screen.dart                     │
│    Метод: _handleRouteResult()                       │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ 2. СОХРАНЕНИЕ В SQLITE (ОБЯЗАТЕЛЬНО)                 │
│    await OfflineOrdersService.instance.saveOrder()   │
│    Статус: isSynced = 0                              │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ 3. ПРОВЕРКА ИНТЕРНЕТА                                │
│    OrdersSyncService.instance.hasInternetConnection()│
└───────┬────────────────────────────────┬─────────────┘
        │                                │
        │ ✅ Есть интернет               │ ❌ Нет интернета
        ↓                                ↓
┌──────────────────────────────┐  ┌──────────────────────────┐
│ 4a. ПОПЫТКА ОТПРАВКИ         │  │ 4b. ПРОПУСК              │
│     В FIREBASE               │  │     Заказ остаётся       │
│     (timeout 3 сек)          │  │     isSynced = 0         │
└───────┬──────────────────────┘  └──────────────────────────┘
        │
        ↓
┌──────────────────────────────────────────────────────┐
│ 5. РЕЗУЛЬТАТ                                         │
│    ✅ Успешно → markAsSynced(orderId)                │
│    ❌ Ошибка → оставить isSynced = 0                 │
└──────────────────────────────────────────────────────┘
        │
        ↓
┌──────────────────────────────────────────────────────┐
│ 6. ФОНОВАЯ СИНХРОНИЗАЦИЯ                            │
│    OrdersSyncService мониторит интернет              │
│    При появлении → отправит все isSynced = 0         │
└──────────────────────────────────────────────────────┘
```

### Поток 2: Просмотр заказов клиентом

```
┌──────────────────────────────────────────────────────┐
│ КЛИЕНТ открывает "Мои заказы"                       │
│    lib/features/orders/screens/orders_screen.dart    │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ Загрузка из SQLite                                   │
│    OfflineOrdersService.instance.getAllOrders()      │
│    Фильтр: WHERE userId = текущийПользователь       │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ Отображение списка                                   │
│    ListView с карточками заказов                     │
└──────────────────────────────────────────────────────┘
```

### Поток 3: Просмотр заказов диспетчером (текущая реализация)

```
┌──────────────────────────────────────────────────────┐
│ ДИСПЕТЧЕР открывает "Все заказы"                    │
│    lib/features/orders/screens/orders_screen.dart    │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ Загрузка из SQLite                                   │
│    OfflineOrdersService.instance.getAllOrders()      │
│    ⚠️ ПРОБЛЕМА: Видит только локальные заказы       │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ Отображение списка                                   │
│    ❌ НЕ ВИДИТ заказы от клиентов!                   │
└──────────────────────────────────────────────────────┘
```

### Поток 4: Просмотр заказов диспетчером (после Firebase)

```
┌──────────────────────────────────────────────────────┐
│ ДИСПЕТЧЕР открывает "Все заказы"                    │
│    lib/features/orders/screens/orders_screen.dart    │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ РЕАЛ-ТАЙМ ПОДПИСКА НА FIREBASE                       │
│    FirebaseOrdersService.instance.getOrdersStream()  │
│    StreamBuilder автоматически обновляет UI          │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ Получение обновлений в РЕАЛЬНОМ ВРЕМЕНИ             │
│    📡 Новый заказ → автоматически появляется         │
│    📡 Статус изменён → автоматически обновляется     │
│    📡 Заказ удалён → автоматически исчезает          │
└───────────────────────┬──────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────┐
│ Кеширование в SQLite (опционально)                  │
│    Для офлайн доступа к истории                     │
└──────────────────────────────────────────────────────┘
```

---

## 🔧 ИЗМЕНЕНИЯ ДЛЯ ПОЛНОЙ ИНТЕГРАЦИИ

### Файл 1: `lib/features/orders/screens/orders_screen.dart`

**Текущая реализация (офлайн):**
```dart
Future<void> _loadData() async {
  // Загружаем из SQLite
  final bookings = await BookingService().getActiveBookings();
  setState(() => _bookings = bookings);
}
```

**Новая реализация (с Firebase):**
```dart
Stream<List<TaxiOrder>>? _ordersStream;

@override
void initState() {
  super.initState();
  _loadUserType();
  _initOrdersStream();  // Подписка на Firebase
}

void _initOrdersStream() async {
  final user = await AuthService.instance.getCurrentUser();
  
  if (user?.userType == UserType.dispatcher) {
    // Диспетчер видит ВСЕ заказы в реальном времени
    setState(() {
      _ordersStream = FirebaseOrdersService.instance.getOrdersStream();
    });
  } else {
    // Клиент видит ТОЛЬКО свои заказы
    setState(() {
      _ordersStream = FirebaseOrdersService.instance
          .getOrdersStream()
          .map((orders) => orders.where((o) => o.userId == user?.id).toList());
    });
  }
}

@override
Widget build(BuildContext context) {
  return StreamBuilder<List<TaxiOrder>>(
    stream: _ordersStream,
    builder: (context, snapshot) {
      if (snapshot.connectionState == ConnectionState.waiting) {
        return Center(child: CupertinoActivityIndicator());
      }
      
      if (!snapshot.hasData || snapshot.data!.isEmpty) {
        return Center(child: Text('Нет заказов'));
      }
      
      final orders = snapshot.data!;
      
      return ListView.builder(
        itemCount: orders.length,
        itemBuilder: (context, index) {
          return OrderCard(order: orders[index]);
        },
      );
    },
  );
}
```

### Файл 2: `lib/features/main_screen.dart`

**Изменение метода создания заказа:**

```dart
// ПОСЛЕ создания заказа в SQLite:
await OfflineOrdersService.instance.saveOrder(order);

// ДОБАВИТЬ попытку быстрой отправки:
try {
  final hasInternet = await OrdersSyncService.instance.hasInternetConnection();
  
  if (hasInternet) {
    print('☁️ [ORDER] Интернет присутствует, отправляем в Firebase...');
    
    // Попытка с таймаутом 3 секунды
    await FirebaseOrdersService.instance.saveOrder(order).timeout(
      const Duration(seconds: 3),
      onTimeout: () {
        print('⏱️ [ORDER] Firebase timeout, синхронизация позже');
        throw TimeoutException('Firebase save timeout');
      },
    );
    
    // Успешно отправлено
    print('✅ [ORDER] Заказ отправлен в Firebase');
    await OfflineOrdersService.instance.markAsSynced(order.orderId);
  } else {
    print('⚠️ [ORDER] Нет интернета, заказ синхронизируется позже');
  }
} catch (e) {
  print('⚠️ [ORDER] Ошибка отправки в Firebase: $e');
  // Заказ останется в локальной базе с isSynced = 0
  // Автосинхронизация отправит его позже
}

// Продолжаем работу приложения (навигация, уведомления и т.д.)
```

### Файл 3: `lib/main.dart`

**Текущий код (закомментировано):**
```dart
// import 'package:firebase_core/firebase_core.dart';
// import 'firebase_options.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // await Firebase.initializeApp(
  //   options: DefaultFirebaseOptions.currentPlatform,
  // );
  
  runApp(MyApp());
}
```

**Новый код (активировано):**
```dart
import 'package:firebase_core/firebase_core.dart';
import 'firebase_options.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Инициализация Firebase
  await Firebase.initializeApp(
    options: DefaultFirebaseOptions.currentPlatform,
  );
  
  // Запуск автосинхронизации
  OrdersSyncService.instance.startAutoSync();
  
  runApp(MyApp());
}
```

---

## 📡 РЕАЛ-ТАЙМ ОБНОВЛЕНИЯ

### Как работает StreamBuilder

```dart
StreamBuilder<List<TaxiOrder>>(
  stream: FirebaseOrdersService.instance.getOrdersStream(),
  builder: (context, snapshot) {
    // Этот builder вызывается КАЖДЫЙ РАЗ при изменении данных в Firestore
    
    // snapshot.data содержит АКТУАЛЬНЫЙ список заказов
    final orders = snapshot.data ?? [];
    
    return ListView.builder(
      itemCount: orders.length,
      itemBuilder: (context, index) => OrderCard(order: orders[index]),
    );
  },
)
```

### Сценарии реал-тайм обновлений:

1. **Новый заказ создан:**
   ```
   Клиент → Firebase → StreamBuilder → UI обновляется
   Время: ~1-2 секунды
   ```

2. **Статус изменён:**
   ```
   Диспетчер → Firebase → StreamBuilder → UI обновляется
   Время: ~1-2 секунды
   ```

3. **Заказ удалён:**
   ```
   Диспетчер → Firebase → StreamBuilder → Заказ исчезает из UI
   Время: ~1-2 секунды
   ```

---

## 🔐 БЕЗОПАСНОСТЬ ДАННЫХ

### Firebase Security Rules

```javascript
// Диспетчер может читать ВСЕ заказы:
allow read: if isDispatcher();

// Клиент может читать ТОЛЬКО свои:
allow read: if request.auth.uid == resource.data.userId;

// Функция проверки роли:
function isDispatcher() {
  return get(/databases/$(database)/documents/users/$(request.auth.uid))
    .data.userType == 'dispatcher';
}
```

### Коллекция пользователей (users):

```
users/
└── {userId}/
    ├── id: "user-123"
    ├── firstName: "Иван"
    ├── lastName: "Петров"
    ├── phone: "+79001234567"
    └── userType: "client" | "dispatcher" ⭐
```

---

## 📊 СТАТИСТИКА И МОНИТОРИНГ

### Firebase Console показывает:

1. **Операции чтения/записи:**
   - Сколько запросов в день
   - Использование квоты
   - График активности

2. **Активные подключения:**
   - Количество онлайн пользователей
   - Реал-тайм подписки

3. **Ошибки:**
   - Отклонённые запросы (Security Rules)
   - Таймауты
   - Неверные запросы

4. **Хранилище:**
   - Размер базы данных
   - Количество документов
   - Использование места

---

## 🚀 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизация запросов:

1. **Индексы (автоматические):**
   ```
   Firestore автоматически создаёт индексы для:
   - Одиночных полей
   - Сортировки по одному полю
   ```

2. **Индексы (составные):**
   ```
   Для сложных запросов (где + сортировка):
   - Firebase предложит создать индекс
   - Нажмите на ссылку в ошибке
   - Индекс создастся за 2-5 минут
   ```

3. **Пагинация (для больших списков):**
   ```dart
   _firestore
     .collection('orders')
     .orderBy('timestamp', descending: true)
     .limit(50)  // Загружаем только 50 последних
     .snapshots();
   ```

4. **Офлайн кеш:**
   ```dart
   // Firestore автоматически кеширует данные локально
   // При потере интернета приложение продолжает работать
   
   FirebaseFirestore.instance.settings = const Settings(
     persistenceEnabled: true,  // Включить кеш
     cacheSizeBytes: Settings.CACHE_SIZE_UNLIMITED,
   );
   ```

---

## 🐛 ОТЛАДКА И ЛОГИРОВАНИЕ

### Включить подробные логи Firebase:

```dart
// В main.dart:
void main() async {
  // ...
  
  // Включить подробные логи Firestore
  FirebaseFirestore.instance.settings = const Settings(
    persistenceEnabled: true,
    cacheSizeBytes: Settings.CACHE_SIZE_UNLIMITED,
  );
  
  // Включить логи Firebase (только для debug)
  if (kDebugMode) {
    await FirebaseFirestore.instance.clearPersistence();
  }
  
  // ...
}
```

### Логи в консоли:

```
✅ Успешные операции:
[cloud_firestore/default] Get document orders/123
[cloud_firestore/default] Document orders/123 cached

❌ Ошибки:
[cloud_firestore/default] PERMISSION_DENIED: Missing or insufficient permissions
[cloud_firestore/default] NOT_FOUND: Document not found
```

---

## 📦 ИТОГОВАЯ СТРУКТУРА ПРОЕКТА

```
lib/
├── services/
│   ├── firebase_orders_service.dart      ☁️ Firebase Firestore
│   ├── offline_orders_service.dart       💾 SQLite локально
│   └── orders_sync_service.dart          🔄 Автосинхронизация
│
├── features/
│   └── orders/
│       └── screens/
│           └── orders_screen.dart        📱 UI с реал-тайм
│
├── main.dart                             🚀 Инициализация Firebase
└── firebase_options.dart                 ⚙️ Конфигурация (auto-generated)
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ РАЗРАБОТЧИКА

### Код готов:
- [x] `FirebaseOrdersService` реализован
- [x] `OrdersSyncService` реализован
- [x] `OfflineOrdersService` реализован
- [x] Автосинхронизация запускается в `main.dart`
- [x] Модель `TaxiOrder` поддерживает `toMap()` и `fromMap()`

### Требуется для запуска:
- [ ] Firebase проект создан
- [ ] `google-services.json` добавлен (Android)
- [ ] `GoogleService-Info.plist` добавлен (iOS)
- [ ] `flutterfire configure` выполнена
- [ ] Firebase импорты раскомментированы в `main.dart`
- [ ] Firestore база создана
- [ ] Security Rules настроены

### Нужно изменить:
- [ ] `orders_screen.dart` → использовать `StreamBuilder` для диспетчера
- [ ] Добавить коллекцию `users` в Firestore с полем `userType`
- [ ] Настроить Firebase Authentication (телефон или анонимный вход)

---

## 🎯 РЕЗУЛЬТАТ

После внедрения:

```
ТЕКУЩЕЕ СОСТОЯНИЕ:
❌ Клиент и диспетчер видят разные данные (каждый свои локальные)
❌ Новые заказы НЕ появляются у диспетчера автоматически
❌ Нужно вручную обновлять список

ПОСЛЕ FIREBASE:
✅ Диспетчер видит ВСЕ заказы от всех клиентов
✅ Новые заказы появляются МГНОВЕННО без обновления
✅ Изменения статуса видны всем в реальном времени
✅ Работает с любого количества устройств одновременно
```

**Время внедрения:** ~75 минут  
**Стоимость:** БЕСПЛАТНО  
**Сложность:** НИЗКАЯ (основной код уже готов)
