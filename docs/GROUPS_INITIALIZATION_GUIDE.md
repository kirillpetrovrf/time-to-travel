# ⚠️ ВАЖНО: КАК ИНИЦИАЛИЗИРОВАТЬ ГРУППЫ

## Проблема
Вы зашли в "Группы маршрутов" и увидели "0 групп".

## Причина
Группы еще не созданы в базе данных Firebase.

## Решение (2 способа)

### **Способ 1: Через кнопку в приложении** ✅ ПРОЩЕ
1. Откройте админ-панель → "Управление маршрутами"
2. Нажмите на иконку **📦** справа вверху
3. Увидите экран "Нет групп маршрутов"
4. Нажмите кнопку **"Инициализировать 9 групп"**
5. Готово! Увидите 9 групп маршрутов

### **Способ 2: Из кода**
Добавьте в `main.dart` перед `runApp()`:

```dart
import 'package:time_to_travel/data/route_groups_initializer.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Инициализировать группы один раз
  await RouteGroupsInitializer.initializeGroups();
  
  runApp(MyApp());
}
```

---

## После инициализации

Вы увидите **9 групп**:
1. Ростовская область (8000₽)
2. Крым (45000₽)
3. Азовское море (7500₽)
4. Черноморское побережье (40000₽)
5. Сочи (50000₽)
6. Краснодар (30000₽)
7. Волгоград, Минводы (40000₽)
8. Ейск (22000₽)
9. Дальняя Россия (50000₽)

**Можно начинать работать!** 🚀

---

## Примечание о Firebase
Если видите ошибку `PERMISSION_DENIED` - это нормально, если Firebase не настроен. 
Система работает локально через SQLite.
