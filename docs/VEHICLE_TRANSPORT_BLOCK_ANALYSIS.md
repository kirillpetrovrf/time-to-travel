# 🔍 АНАЛИЗ: Почему не отображается блок "Транспорт" в деталях заказа

## 📊 Анализ логов

### Ключевая информация из логов:

```
💰 [INDIVIDUAL] ========== РАСЧЕТ ИТОГОВОЙ ЦЕНЫ ==========
💰 [INDIVIDUAL] Базовая цена: 8000₽
💰 [INDIVIDUAL] Транспорт: +0₽  ← ПРОБЛЕМА: транспорт = 0₽
💰 [INDIVIDUAL] Багаж: 0₽
💰 [INDIVIDUAL] Животные: 2000₽
💰 [INDIVIDUAL] VK скидка: -0₽
💰 [INDIVIDUAL] ИТОГО: 10000₽
```

**Вывод:** `_selectedVehicleClass` = `null` при создании заказа

### JSON бронирования показывает:

```json
"vehicleClass": null  ← Не указан тип транспорта
```

---

## 🔍 Причина проблемы

### 1. **Пользователь НЕ ВЫБРАЛ тип транспорта при бронировании**

**Проверка кода:**

- ✅ UI для выбора транспорта существует: `_buildVehicleSection()` на строке 1960
- ✅ Кнопка отображается на строке 199: `_buildVehicleSection(theme)`
- ✅ Логика сохранения работает: строка 1863 `vehicleClass: _selectedVehicleClass?.name`
- ✅ Блок отображения существует: `_buildVehicleClassCard()` на строке 439 в `booking_detail_screen.dart`

**НО:**
```dart
Widget _buildVehicleClassCard(theme) {
  if (_currentBooking.vehicleClass == null || _currentBooking.vehicleClass!.isEmpty) {
    return const SizedBox.shrink(); // ← БЛОК НЕ ПОКАЗЫВАЕТСЯ если vehicleClass == null
  }
  // ...
}
```

---

## 🆚 Сравнение: Индивидуальный трансфер vs Свободный маршрут

### Индивидуальный трансфер (`individual_booking_screen.dart`):

**Текущее состояние:**
```dart
// Строка 199 - блок присутствует
_buildVehicleSection(theme),

// Строка 1960 - виджет
Widget _buildVehicleSection(theme) {
  return Container(
    // ...
    child: CupertinoButton(
      onPressed: _openVehicleSelection,
      child: // ... текст "Выберите тип транспорта" или название выбранного
    ),
  );
}

// Строка 1863 - сохранение
vehicleClass: _selectedVehicleClass?.name, // ← null если не выбран
```

**Проблема:** Поле **опциональное**, пользователь может не выбирать транспорт → vehicleClass сохраняется как `null`

### Свободный маршрут (`custom_route_booking_modal.dart`):

```dart
// Строка 152 - сохранение
vehicleClass: _selectedVehicleClass?.name,

// Строка 1060-1139 - UI
Widget _buildVehicleSelection(theme) {
  return Container(
    child: CupertinoButton(
      onPressed: () async {
        final result = await Navigator.push<VehicleClass>(
          context,
          CupertinoPageRoute(
            builder: (context) => VehicleSelectionScreen(
              selectedVehicleClass: _selectedVehicleClass,
            ),
          ),
        );
        if (result != null) {
          setState(() => _selectedVehicleClass = result);
        }
      },
      child: // ... отображение с ценой
    ),
  );
}
```

**Вывод:** Та же проблема - поле опциональное, если не выбран → `null`

---

## 📋 Что отображается в деталях заказа

### booking_detail_screen.dart:

```dart
// Строка 60 - блок ВСЕГДА вызывается
_buildVehicleClassCard(theme),

// Строка 439 - реализация
Widget _buildVehicleClassCard(theme) {
  if (_currentBooking.vehicleClass == null || _currentBooking.vehicleClass!.isEmpty) {
    return const SizedBox.shrink(); // ← НЕ ПОКАЗЫВАЕТСЯ
  }
  
  // Извлекаем название и цену
  String vehicleName;
  int extraPrice;
  
  switch (_currentBooking.vehicleClass) {
    case 'sedan':
      vehicleName = 'Седан (до 4 мест)';
      extraPrice = 0;
      break;
    case 'wagon':
      vehicleName = 'Универсал (до 4 мест)';
      extraPrice = 2000;
      break;
    case 'minivan':
      vehicleName = 'Минивэн (до 7 мест)';
      extraPrice = 4000;
      break;
    case 'microbus':
      vehicleName = 'Микроавтобус (до 18 мест)';
      extraPrice = 8000;
      break;
    default:
      vehicleName = _currentBooking.vehicleClass!;
      extraPrice = 0;
  }
  
  return Container(
    // ... карточка с vehicleName и extraPrice
    child: Column(
      children: [
        Row(
          children: [
            Icon(CupertinoIcons.car_detailed, color: theme.primary),
            const SizedBox(width: 8),
            Text('Транспорт', style: ...),
          ],
        ),
        Row(
          children: [
            Text(vehicleName, style: ...),
            if (extraPrice > 0)
              Text('+$extraPrice ₽', style: ...), // ← Показывает цену
          ],
        ),
      ],
    ),
  );
}
```

---

## 🎯 Проблема и её решение

### Текущая ситуация:

1. **Поле `vehicleClass` опциональное** - можно не выбирать
2. **Если не выбран** → сохраняется как `null`
3. **В деталях заказа** → блок не показывается (`return SizedBox.shrink()`)
4. **В итоговой цене** → `Транспорт: +0₽`

### Возможные решения:

#### ❌ **НЕПРАВИЛЬНОЕ решение:** Сделать поле обязательным
- Усложнит UX
- Не всегда нужен выбор (например, по умолчанию седан)

#### ✅ **ПРАВИЛЬНОЕ решение 1:** Установить значение по умолчанию

**Изменить в `individual_booking_screen.dart`:**

```dart
// В initState или при создании booking
_selectedVehicleClass = VehicleClass.sedan; // По умолчанию седан (0₽)
```

**Плюсы:**
- ✅ Всегда будет указан тип транспорта
- ✅ Седан = 0₽ (не влияет на цену)
- ✅ Блок всегда показывается в деталях
- ✅ Пользователь видит что выбран седан

**Минусы:**
- ⚠️ Изменяет текущее поведение (сейчас можно не выбирать)

#### ✅ **ПРАВИЛЬНОЕ решение 2:** Показывать блок даже если vehicleClass = null

**Изменить `_buildVehicleClassCard` в `booking_detail_screen.dart`:**

```dart
Widget _buildVehicleClassCard(theme) {
  // Убрать эту проверку:
  // if (_currentBooking.vehicleClass == null || _currentBooking.vehicleClass!.isEmpty) {
  //   return const SizedBox.shrink();
  // }
  
  // Вместо этого:
  String vehicleName;
  int extraPrice;
  
  if (_currentBooking.vehicleClass == null || _currentBooking.vehicleClass!.isEmpty) {
    vehicleName = 'Седан (по умолчанию)';
    extraPrice = 0;
  } else {
    // существующая логика switch
  }
  
  // остальной код без изменений
}
```

**Плюсы:**
- ✅ Минимальные изменения
- ✅ Блок всегда показывается
- ✅ Если не выбран → показывается "Седан (по умолчанию) +0₽"

**Минусы:**
- ⚠️ Может показывать неточную информацию (если вдруг не седан)

#### ✅ **ПРАВИЛЬНОЕ решение 3 (РЕКОМЕНДУЕТСЯ):** Комбинация 1 + 2

1. **В `individual_booking_screen.dart`** - установить седан по умолчанию:
   ```dart
   VehicleClass? _selectedVehicleClass = VehicleClass.sedan; // По умолчанию седан
   ```

2. **В `booking_detail_screen.dart`** - показывать блок всегда:
   ```dart
   Widget _buildVehicleClassCard(theme) {
     String vehicleName;
     int extraPrice;
     
     final vehicleClassStr = _currentBooking.vehicleClass ?? 'sedan'; // Fallback
     
     switch (vehicleClassStr) {
       case 'sedan':
         vehicleName = 'Седан (до 4 мест)';
         extraPrice = 0;
         break;
       // ... остальные case
     }
     
     // показываем карточку
   }
   ```

**Плюсы:**
- ✅ Всегда указан транспорт
- ✅ Блок всегда показывается
- ✅ Корректная информация
- ✅ Седан по умолчанию = 0₽ (не влияет на цену)
- ✅ Пользователь явно видит что за транспорт

---

## 📊 Что показывается в заказах сейчас

### Индивидуальный трансфер:
```
✅ Статус
✅ Маршрут (откуда → куда)
✅ Адреса (точные адреса)
✅ Дата и время
✅ Пассажиры (3: 2 взрослых, 1 ребенок)
❌ Транспорт (НЕ ПОКАЗЫВАЕТСЯ если не выбран) ← ПРОБЛЕМА
✅ Багаж (S=1, M=1, L=2)
✅ Животные (1 собака)
✅ Комментарий
✅ Стоимость (10000₽)
```

### Свободный маршрут (для сравнения):
```
✅ Статус
✅ Маршрут
✅ Адреса
✅ Дата и время
✅ Пассажиры
✅ Транспорт (если выбран) ← Та же проблема если не выбран
✅ Багаж
✅ Животные
✅ Комментарий
✅ Стоимость
```

---

## 🎯 Рекомендация

**Внедрить Решение 3 (комбинированное):**

### Шаг 1: Установить седан по умолчанию

**Файл:** `lib/features/booking/screens/individual_booking_screen.dart`

**Строка ~38:**
```dart
// БЫЛО:
VehicleClass? _selectedVehicleClass;

// СТАЛО:
VehicleClass? _selectedVehicleClass = VehicleClass.sedan; // По умолчанию седан (0₽)
```

### Шаг 2: Обновить логику отображения блока

**Файл:** `lib/features/orders/screens/booking_detail_screen.dart`

**Строка 439:**
```dart
Widget _buildVehicleClassCard(theme) {
  // УБРАТЬ эту проверку:
  // if (_currentBooking.vehicleClass == null || _currentBooking.vehicleClass!.isEmpty) {
  //   return const SizedBox.shrink();
  // }
  
  // Форматируем название класса транспорта и получаем цену доплаты
  String vehicleName;
  int extraPrice;
  
  // ДОБАВИТЬ fallback на sedan:
  final vehicleClassStr = _currentBooking.vehicleClass ?? 'sedan';
  
  switch (vehicleClassStr) {
    case 'sedan':
      vehicleName = 'Седан (до 4 мест)';
      extraPrice = 0;
      break;
    // ... остальной код без изменений
  }
  
  // показываем карточку (остальной код без изменений)
}
```

### Шаг 3: То же для custom_route_booking_modal.dart

**Файл:** `lib/widgets/custom_route_booking_modal.dart`

**Строка ~55:**
```dart
// БЫЛО:
VehicleClass? _selectedVehicleClass;

// СТАЛО:
VehicleClass? _selectedVehicleClass = VehicleClass.sedan;
```

---

## ✅ Результат после исправления

### Что изменится:

1. **При создании заказа:**
   - ✅ По умолчанию выбран Седан (0₽)
   - ✅ Пользователь может изменить на другой
   - ✅ Всегда сохраняется тип транспорта

2. **В деталях заказа:**
   - ✅ Блок "Транспорт" ВСЕГДА показывается
   - ✅ Отображается выбранный тип или "Седан" по умолчанию
   - ✅ Показывается доплата (0₽ для седана, +2000₽/+4000₽/+8000₽ для остальных)

3. **В логах:**
   ```
   💰 [INDIVIDUAL] Транспорт: +0₽     ← Вместо этого
   💰 [INDIVIDUAL] Транспорт: +0₽ (Седан)   ← Будет понятно что выбран седан
   ```

---

## 🎉 Итого

**Проблема:** Пользователь не выбрал транспорт → `vehicleClass = null` → блок не показывается

**Решение:** Установить Седан по умолчанию + убрать проверку на `null` в `_buildVehicleClassCard`

**Результат:** Блок "Транспорт" будет показываться ВСЕГДА для индивидуальных трансферов и свободных маршрутов
