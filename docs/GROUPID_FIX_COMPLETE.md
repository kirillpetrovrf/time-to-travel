# Исправление сохранения groupId для маршрутов

## Проблема
Когда пользователь добавлял маршрут с выбранной группой "Любые маршруты", маршрут сохранялся, но `groupId` не записывался в базу данных. При фильтрации по группе "Любые маршруты" маршруты не отображались.

## Причина
Было найдено ДВА источника проблемы:

### 1. Неправильная структура БД в `offline_routes_service.dart`
Файл `lib/services/offline_routes_service.dart` использовал:
- **Старые названия колонок**: `from_city`, `to_city`, `created_at`, `updated_at`, `is_synced`
- **Отсутствие колонки `groupId`**: колонка вообще не была добавлена в схему таблицы
- **Неправильный формат данных**: даты хранились как ISO8601 строки вместо milliseconds

### 2. Логика фильтрации не поддерживала groupId
Фильтрация маршрутов в `route_management_widget.dart` проверяла ТОЛЬКО совпадение городов с городами группы. Для группы "Любые маршруты" с пустыми списками городов это не работало.

## Решение

### Часть 1: Исправление структуры БД

#### Обновлено `_onCreate` (создание таблицы):
```dart
CREATE TABLE predefined_routes (
  id TEXT PRIMARY KEY,
  fromCity TEXT NOT NULL,        // было: from_city
  toCity TEXT NOT NULL,           // было: to_city
  price REAL NOT NULL,
  groupId TEXT,                   // ДОБАВЛЕНО
  createdAt INTEGER NOT NULL,     // было: created_at TEXT
  updatedAt INTEGER NOT NULL,     // было: updated_at TEXT
  isSynced INTEGER DEFAULT 0      // было: is_synced
)
```

#### Обновлено `_onUpgrade` (миграция):
Добавлена колонка `groupId` при миграции с версии 1 на версию 2.

#### Обновлено `addRoute` (вставка маршрута):
```dart
final map = {
  'id': routeId,
  'fromCity': route.fromCity,        // было: 'from_city'
  'toCity': route.toCity,             // было: 'to_city'
  'price': route.price,
  'groupId': route.groupId,           // ДОБАВЛЕНО
  'createdAt': route.createdAt.millisecondsSinceEpoch,  // было: toIso8601String()
  'updatedAt': route.updatedAt.millisecondsSinceEpoch,  // было: toIso8601String()
  'isSynced': 0,                      // было: 'is_synced'
};
```

#### Обновлены все методы чтения из БД:
- `getAllRoutes()` - читает с новыми колонками + `groupId`
- `findRoute()` - читает с новыми колонками + `groupId`
- `getUnsyncedRoutes()` - читает с новыми колонками + `groupId`

#### Обновлены методы записи:
- `updateRoute()` - записывает с новыми колонками + `groupId`
- `syncWithFirebase()` - записывает с новыми колонками + `groupId`

#### Обновлены SQL-запросы:
- Статистика (`getRoutesStats()`) - использует новые имена колонок

### Часть 2: Исправление логики фильтрации

**Было**:
```dart
final displayedRoutes = _selectedGroup == null
    ? _routes
    : _routes.where((route) {
        // Только проверка городов
        final fromMatch = _selectedGroup!.originCities.any(...);
        final toMatch = _selectedGroup!.destinationCities.any(...);
        return fromMatch && toMatch;
      }).toList();
```

**Стало**:
```dart
final displayedRoutes = _selectedGroup == null
    ? _routes
    : _routes.where((route) {
        // 1. СНАЧАЛА проверяем явное назначение по groupId
        if (route.groupId != null && route.groupId == _selectedGroup!.id) {
          return true;
        }
        
        // 2. Если у группы нет городов (как у "Любые маршруты")
        if (_selectedGroup!.originCities.isEmpty || 
            _selectedGroup!.destinationCities.isEmpty) {
          return false;  // Только по groupId
        }
        
        // 3. ЗАТЕМ проверяем совпадение городов (старая логика)
        final fromMatch = _selectedGroup!.originCities.any(...);
        final toMatch = _selectedGroup!.destinationCities.any(...);
        
        if (_selectedGroup!.autoGenerateReverse) {
          // проверка обратного направления
        }
        
        return fromMatch && toMatch;
      }).toList();
```

### Часть 3: Добавление диагностики

Добавлено логирование на всех этапах:

1. **RouteManagementWidget** (`_addRoute`):
```dart
print('🔍 [DEBUG] RouteManagementWidget._addRoute():');
print('   groupId to pass: ${_selectedGroupForNewRoute?.id}');
```

2. **RouteManagementService** (`addRoute`):
```dart
print('🔍 [DEBUG] RouteManagementService.addRoute():');
print('   groupId parameter: $groupId');
print('   route.groupId: ${route.groupId}');
```

3. **OfflineRoutesService** (`addRoute`):
```dart
print('🔍 [DEBUG] OfflineRoutesService.addRoute():');
print('   route.groupId: ${route.groupId}');
print('🔍 [DEBUG] SQL map to insert: $map');
```

## Инструкции по тестированию

### 1. Горячий перезапуск приложения
**ВАЖНО**: Чтобы применилась миграция базы данных, нужен **полный перезапуск** (не Hot Reload).

```bash
# Остановить приложение
# Затем запустить заново
flutter run
```

### 2. Проверить миграцию
В логах должно появиться:
```
🔄 [SQLITE_ROUTES] Миграция БД с версии 1 на 2
🗑️ [SQLITE_ROUTES] Удаляем старую таблицу...
🔧 [SQLITE_ROUTES] Создаем новую таблицу с правильной структурой...
✅ [SQLITE_ROUTES] Миграция завершена успешно
```

### 3. Добавить тестовый маршрут
1. Перейти в "Фиксированные цены"
2. Нажать "Добавить новый маршрут"
3. Выбрать адрес отправления: "Москва"
4. Выбрать адрес назначения: "Санкт-Петербург"
5. **ВАЖНО**: Нажать на поле "Группа" и выбрать "Любые маршруты"
6. Ввести цену: 5000
7. Нажать "Добавить маршрут"

### 4. Проверить логи
Должны появиться следующие логи:

```
🔍 [DEBUG] RouteManagementWidget._addRoute():
   fromCity: Москва
   toCity: Санкт-Петербург
   price: 5000.0
   _selectedGroupForNewRoute: Любые маршруты
   groupId to pass: any_routes

🔍 [DEBUG] RouteManagementService.addRoute():
   fromCity: Москва
   toCity: Санкт-Петербург
   price: 5000.0
   groupId parameter: any_routes
   route.groupId: any_routes

🔍 [DEBUG] OfflineRoutesService.addRoute():
   route.groupId: any_routes
🔍 [DEBUG] SQL map to insert: {
  id: route_...,
  fromCity: Москва,
  toCity: Санкт-Петербург,
  price: 5000.0,
  groupId: any_routes,  ← ДОЛЖНО ПРИСУТСТВОВАТЬ!
  createdAt: ...,
  updatedAt: ...,
  isSynced: 0
}

✅ [SQLITE_ROUTES] Маршрут сохранен в SQLite
```

### 5. Проверить фильтрацию
1. В секции "Группы маршрутов" нажать на "Любые маршруты"
2. Должен отобразиться добавленный маршрут "Москва → Санкт-Петербург"
3. Счетчик должен показать "Любые маршруты (1)" вместо "Любые маршруты (0)"

### 6. Проверить старые маршруты
После миграции все старые маршруты будут иметь `groupId = NULL`. Они будут:
- ✅ Видны в "Все маршруты"
- ❌ НЕ видны ни в одной группе (включая "Любые маршруты")

Чтобы назначить старые маршруты к группе, нужно:
1. Открыть маршрут на редактирование
2. Выбрать группу
3. Сохранить

## Измененные файлы

### Критические изменения (БАЗА ДАННЫХ):
1. `/lib/services/offline_routes_service.dart`
   - Обновлена схема таблицы (`_onCreate`)
   - Добавлена миграция (`_onUpgrade`)
   - Исправлены все методы (insert/update/select)
   - Добавлено логирование `groupId`

### Логика фильтрации:
2. `/lib/features/admin/widgets/route_management_widget.dart`
   - Обновлена логика фильтрации (поддержка `groupId`)
   - Добавлено логирование при добавлении маршрута

### Диагностика:
3. `/lib/services/route_management_service.dart`
   - Добавлено логирование `groupId` при создании маршрута

## Ожидаемое поведение после исправления

### ✅ Работает правильно:
- Добавление маршрута с выбранной группой → `groupId` сохраняется в БД
- Фильтрация по группе "Любые маршруты" → показывает маршруты с `groupId='any_routes'`
- Фильтрация по другим группам (например, "Ростовская область") → показывает маршруты:
  - С `groupId='rostov_region'` ИЛИ
  - С подходящими городами из списка группы

### ⚠️ Требует внимания:
- Старые маршруты (добавленные до миграции) имеют `groupId=NULL`
- Они НЕ отображаются при фильтрации по любой группе
- Нужно вручную назначить им группы через редактирование

## Статус
✅ **ИСПРАВЛЕНО И ГОТОВО К ТЕСТИРОВАНИЮ**

Дата: 2025-01-27
