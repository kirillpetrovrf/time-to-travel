# 🧪 Инструкция По Тестированию Синхронизации

## ✅ Что Было Исправлено

**Проблема:** Заказы создавались только локально (SharedPreferences/SQLite), не отправлялись на backend.

**Решение:** Обновлен `BookingService`:
- ✅ Создание заказа → отправка на `https://titotr.ru/api/orders`
- ✅ Загрузка заказов → получение с `https://titotr.ru/api/orders`
- ✅ Синхронизация между телефонами через backend

---

## 📱 Тест 1: Проверка Создания Заказа

### Шаг 1: Подготовка Телефона 1
1. Запустить приложение
2. Включить логирование:
   ```bash
   flutter logs | grep -E "📤|✅|⚠️|❌"
   ```

### Шаг 2: Создать Заказ
1. Открыть свободный маршрут или индивидуальный трансфер
2. Заполнить данные:
   - Откуда: Донецк
   - Куда: Ростов-на-Дону
   - Дата и время
   - Пассажиры: 2 взрослых
   - Багаж: 1 чемодан (L)
3. Нажать "Создать заказ"

### Шаг 3: Проверить Логи
**Ожидаемый результат (с интернетом):**
```
📤 Создание бронирования: сначала отправка на backend API...
🌐 Загрузка заказов с backend API...
✅ Заказ успешно создан на backend с ID: 123e4567-e89b-12d3-a456-426614174000
💾 Бронирование 123e4567-... сохранено локально
🔔 Планирование уведомлений для заказа 123e4567-...
✅ Уведомления успешно запланированы
```

**Ожидаемый результат (без интернета):**
```
📤 Создание бронирования: сначала отправка на backend API...
⚠️ Ошибка отправки на backend: ...
📱 Сохраняем заказ локально для последующей синхронизации
📱 Создано оффлайн бронирование: offline_1704096000000
```

---

## 📱 Тест 2: Проверка Синхронизации На Второй Телефон

### Шаг 1: Подготовка Телефона 2
1. Запустить приложение на втором устройстве
2. **ВАЖНО:** Войти под тем же аккаунтом (если есть авторизация)
3. Включить логирование:
   ```bash
   flutter logs | grep -E "📥|✅|⚠️|❌"
   ```

### Шаг 2: Загрузить Заказы
1. Открыть вкладку "Мои заказы"
2. Потянуть вниз для обновления (если есть pull-to-refresh)

### Шаг 3: Проверить Логи
**Ожидаемый результат:**
```
📥 Загрузка бронирований: сначала с backend, затем локальные...
🌐 Загрузка заказов с backend API...
✅ Получено 1 заказов с backend
✅ Конвертировано 1 заказов с backend
📦 Загружено 0 локальных индивидуальных трансферов
📦 Загружено 0 заказов такси из SQLite
✅ Всего загружено 1 уникальных бронирований
```

### Шаг 4: Проверить UI
**Ожидаемый результат:**
- ✅ Заказ отображается в списке "Мои заказы"
- ✅ Данные корректны:
  - Откуда: Донецк
  - Куда: Ростов-на-Дону
  - Цена: 10000₽
  - Статус: Ожидает подтверждения

---

## 🔍 Отладка: Если Синхронизация Не Работает

### Проблема 1: Заказ не отправляется на backend

#### Проверить:
```bash
flutter logs | grep "Ошибка отправки на backend"
```

#### Возможные причины:
1. **Нет интернета** → заказ сохранен локально с `offline_` префиксом
2. **Backend недоступен** → проверить `https://titotr.ru/health`
3. **Ошибка авторизации** → проверить JWT токены в flutter_secure_storage

#### Решение:
```bash
# Проверить доступность backend
curl -X GET https://titotr.ru/health

# Ожидается:
# {"status":"ok","timestamp":"2024-01-01T12:00:00Z"}
```

---

### Проблема 2: Второй телефон не видит заказ

#### Проверить:
```bash
flutter logs | grep "Загрузка бронирований"
```

#### Возможные причины:
1. **Разные аккаунты** → заказы привязаны к userId
2. **Backend не вернул заказы** → проверить API запрос
3. **Ошибка конвертации** → проверить логи "Конвертировано N заказов"

#### Решение:
```bash
# Проверить API напрямую (если есть токен)
curl -X GET https://titotr.ru/api/orders \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"

# Ожидается:
# {"orders":[{"id":"123...","userId":"...","fromAddress":"Донецк",...}],"count":1}
```

---

### Проблема 3: Токен истек (401 Unauthorized)

#### Проверить:
```bash
flutter logs | grep "401"
```

#### Решение:
API Client автоматически обновляет токены, но если это не работает:
```dart
// lib/services/api/api_client.dart
// Строка ~80-100: автоматическое обновление токена

if (response.statusCode == 401) {
  await _refreshToken();
  // Повторяем запрос
}
```

---

## 🛠️ Дополнительные Команды

### Очистить локальные данные (для чистого теста)
```dart
// В приложении (например, в настройках)
final prefs = await SharedPreferences.getInstance();
await prefs.remove('offline_bookings');
await OfflineOrdersService.instance.clearAllOrders();

debugPrint('✅ Локальные данные очищены');
```

### Проверить сохраненные токены
```dart
import 'package:flutter_secure_storage/flutter_secure_storage.dart';

final storage = FlutterSecureStorage();
final accessToken = await storage.read(key: 'access_token');
final refreshToken = await storage.read(key: 'refresh_token');

debugPrint('Access Token: ${accessToken?.substring(0, 20)}...');
debugPrint('Refresh Token: ${refreshToken?.substring(0, 20)}...');
```

---

## ✅ Чек-лист Успешного Теста

### Телефон 1 (Создание Заказа)
- [ ] Заказ создан в UI
- [ ] Лог: "✅ Заказ успешно создан на backend с ID: ..."
- [ ] Лог: "💾 Бронирование ... сохранено локально"
- [ ] Заказ отображается в "Мои заказы"

### Телефон 2 (Загрузка Заказа)
- [ ] Открыта вкладка "Мои заказы"
- [ ] Лог: "✅ Получено 1 заказов с backend"
- [ ] Лог: "✅ Всего загружено 1 уникальных бронирований"
- [ ] Заказ отображается в списке
- [ ] Данные заказа корректны (адреса, цена, дата)

---

## 🎯 Следующие Шаги После Успешного Теста

1. **Добавить Регистрацию/Логин**
   - Экран регистрации
   - Экран входа
   - Сохранение userId в приложении

2. **Создать OfflineSyncService**
   - Автоматическая синхронизация при появлении интернета
   - Повторная отправка offline_ заказов

3. **Telegram Уведомления**
   - Настроить Bot Token
   - Отправлять уведомления диспетчеру при новом заказе

4. **Admin Panel**
   - Список всех заказов (для диспетчера)
   - Подтверждение/отмена заказов
   - Статистика

---

## 📞 Помощь

Если что-то не работает, проверьте:
1. Логи приложения: `flutter logs`
2. Backend здоровье: `curl https://titotr.ru/health`
3. Файлы интеграции:
   - `lib/services/booking_service.dart` (обновлен)
   - `lib/services/api/orders_api_service.dart` (создан ранее)
   - `lib/services/api/api_client.dart` (создан ранее)

Все готово для теста! 🚀
