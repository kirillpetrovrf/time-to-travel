# 🔧 Исправление проблем при повторном запуске приложения

## Описание проблем

После повторного запуска приложения (когда оно уже было установлено через `flutter run`) возникали следующие проблемы:

1. **Клавиатура закрывается** - при попытке ввести текст в адресные строки клавиатура быстро открывается и сразу закрывается
2. **Геолокация заблокирована** - при нажатии на кнопку геолокации выдает ошибку "Разрешение на геолокацию заблокировано. Включите в настройках"
3. **Приложение сворачивается** - через какое-то время приложение само сворачивается
4. **Нет автоматического запроса разрешений** - приложение не запрашивало разрешение на геолокацию при первом запуске

## Проведенные исправления

### 0. Автоматический запрос разрешений при первом запуске

**Файл:** `lib/main.dart`

**ВАЖНО:** Добавлен автоматический запрос разрешений на геолокацию при запуске приложения!

```dart
void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Инициализация Yandex MapKit
  await init.initMapkit(apiKey: kYandexMapKitApiKey);
  
  // 🔐 Запрос разрешений на геолокацию при первом запуске
  await _requestLocationPermissionOnStartup();
  
  runApp(const TaxiRouteCalculatorApp());
}

/// Запрашиваем разрешение на геолокацию при первом запуске приложения
Future<void> _requestLocationPermissionOnStartup() async {
  try {
    final permission = await geolocator.Geolocator.checkPermission();
    
    // Если разрешение не предоставлено, запрашиваем его
    if (permission == geolocator.LocationPermission.denied) {
      print('📱 Запрашиваем разрешение на геолокацию...');
      final result = await geolocator.Geolocator.requestPermission();
      
      if (result == geolocator.LocationPermission.whileInUse || 
          result == geolocator.LocationPermission.always) {
        print('✅ Разрешение на геолокацию предоставлено!');
      }
    }
  } catch (e) {
    print('❌ Ошибка при запросе разрешений: $e');
    // Не прерываем запуск приложения
  }
}
```

**Теперь приложение:**
- ✅ Спрашивает разрешение **один раз** при первом запуске
- ✅ Сохраняет выбор пользователя навсегда
- ✅ При повторных запусках не беспокоит пользователя
- ✅ Если пользователь отклонил - можно разрешить через кнопку геолокации

### 1. Проблема с клавиатурой (TextField Focus)

**Файл:** `lib/widgets/search_field_with_suggestions.dart`

**Изменения:**
- Добавлен флаг `_isDisposing` для предотвращения операций при уничтожении виджета
- Улучшена обработка фокуса в `_focusNode.addListener`
- Добавлены проверки `mounted` и `_isDisposing` перед всеми операциями с overlay
- Добавлен параметр `canRequestFocus: true` в TextField
- Добавлен обработчик `onTapOutside` который НЕ закрывает клавиатуру
- Добавлено логирование для отладки состояния фокуса

```dart
// Флаг для предотвращения операций при dispose
bool _isDisposing = false;

@override
void initState() {
  super.initState();
  _focusNode = FocusNode();
  _fieldKey = GlobalKey();
  
  _focusNode.addListener(() {
    if (_isDisposing || !mounted) return;
    
    if (_focusNode.hasFocus) {
      print('🎯 TextField получил фокус');
      widget.onFieldTapped?.call();
      WidgetsBinding.instance.addPostFrameCallback((_) {
        if (!_isDisposing && mounted && _focusNode.hasFocus) {
          _showOverlay();
        }
      });
    } else {
      print('🎯 TextField потерял фокус');
      _hideOverlay();
    }
  });
}

// В TextField добавлено:
onTapOutside: (event) {
  // 🔧 НЕ закрываем клавиатуру при тапе вне поля
  print('🎯 TextField onTapOutside - НЕ закрываем клавиатуру');
},
```

### 2. Проблема с геолокацией

**Файл:** `lib/features/main_screen.dart`

**Изменения:**

#### 2.1. Улучшен `initState()` - НЕ запускаем проверки при старте
```dart
@override
void initState() {
  super.initState();
  
  // 🔧 Улучшенный lifecycle listener - НЕ запускаем проверки при resume
  _lifecycleListener = AppLifecycleListener(
    onResume: () {
      print('📱 App resumed - NOT triggering permission checks to avoid crashes');
      // НЕ вызываем проверки разрешений здесь!
      // Это может вызвать конфликты состояния и закрытие клавиатуры
    },
  );

  // 🔧 Проверяем разрешения только при первом запуске, БЕЗ автозапроса
  // Используем отложенную проверку чтобы UI успел инициализироваться
  Future.delayed(const Duration(milliseconds: 500), () {
    if (mounted) {
      _checkLocationPermissions(); // Только проверяем, НЕ запрашиваем
    }
  });
}
```

#### 2.2. Улучшен метод `_moveToUserLocation()` с правильной обработкой разрешений

```dart
// Проверяем разрешения с детальной обработкой каждого случая
if (permission == geolocator.LocationPermission.deniedForever) {
  print('❌ Геолокация: Разрешение отклонено навсегда');
  if (mounted) {
    // Показываем диалог с объяснением
    await showDialog(
      context: context,
      builder: (BuildContext context) {
        return AlertDialog(
          title: const Text('Требуется разрешение'),
          content: const Text(
            'Для использования геолокации необходимо разрешить доступ к местоположению в настройках приложения.',
          ),
          actions: [
            TextButton(
              onPressed: () => Navigator.of(context).pop(),
              child: const Text('Отмена'),
            ),
            TextButton(
              onPressed: () async {
                Navigator.of(context).pop();
                await geolocator.Geolocator.openAppSettings();
              },
              child: const Text('Открыть настройки'),
            ),
          ],
        );
      },
    );
  }
  return;
}

// Добавлены проверки mounted перед всеми showSnackBar
if (mounted) {
  showSnackBar(context, 'Перемещено к вашему местоположению ✅');
}

// Добавлена детальная обработка исключений
} on geolocator.LocationServiceDisabledException catch (e) {
  print('❌ Сервис геолокации отключен: $e');
  if (mounted) {
    showSnackBar(context, 'Включите службу геолокации');
  }
} on geolocator.PermissionDeniedException catch (e) {
  print('❌ Разрешение отклонено: $e');
  if (mounted) {
    showSnackBar(context, 'Доступ к геолокации отклонен');
  }
}
```

#### 2.3. Удалены неиспользуемые компоненты
- Удален `PermissionManager` - используем `geolocator` напрямую для лучшего контроля
- Удален `DialogsFactory` - используем стандартные диалоги Flutter
- Удалены неиспользуемые импорты

### 3. Проблема со сворачиванием приложения

**Причина:** `AppLifecycleListener.onResume` вызывал `_requestPermissionsIfNeeded()` при каждом возврате в приложение, что могло вызывать конфликты состояния.

**Решение:** Полностью убрали автоматические проверки разрешений из `onResume`:

```dart
_lifecycleListener = AppLifecycleListener(
  onResume: () {
    print('📱 App resumed - NOT triggering permission checks to avoid crashes');
    // Ничего не делаем при resume - это предотвращает конфликты
  },
);
```

## Результат

✅ **Клавиатура остается открытой** - TextField сохраняет фокус корректно  
✅ **Геолокация работает** - правильная обработка разрешений с понятными диалогами  
✅ **Приложение стабильно** - не сворачивается и не вызывает конфликты состояния  
✅ **Улучшенная отладка** - добавлено детальное логирование для диагностики

## Тестирование

1. **Установите приложение:**
   ```bash
   flutter run
   ```

2. **Закройте приложение и откройте снова**

3. **Проверьте клавиатуру:**
   - Нажмите на поле "Откуда"
   - Клавиатура должна остаться открытой
   - Начните вводить текст - клавиатура НЕ должна закрыться

4. **Проверьте геолокацию:**
   - Нажмите кнопку геолокации (справа внизу)
   - Если разрешение не предоставлено - появится диалог с предложением открыть настройки
   - После предоставления разрешения геолокация должна работать

5. **Проверьте стабильность:**
   - Используйте приложение 5-10 минут
   - Сворачивайте и разворачивайте приложение
   - Приложение НЕ должно само сворачиваться

## Логи для отладки

Обращайте внимание на следующие сообщения в логах:

- `🎯 TextField получил фокус` - TextField активировался
- `🎯 TextField onTapOutside - НЕ закрываем клавиатуру` - тап вне поля обработан правильно
- `📱 App resumed - NOT triggering permission checks` - приложение вернулось без проверок
- `📍 Статус разрешения геолокации: ...` - текущее состояние разрешений
- `✅ Камера перемещена успешно` - геолокация сработала

## Важные моменты

1. **Не проверяем разрешения автоматически** при каждом `onResume` - это вызывало проблемы
2. **Используем `mounted` проверки** перед всеми операциями с UI
3. **Добавлен `_isDisposing` флаг** для предотвращения операций при уничтожении виджета
4. **Детальная обработка всех случаев** с разрешениями геолокации
5. **Логирование** помогает быстро диагностировать проблемы
